---
id: T-0013
title: Fix the tsc error in tests/screen.test.ts that breaks scripts/check.sh
priority: P0
complexity: C1
depends_on: []
scope: [tests/screen.test.ts]
---

## Goal
`npx tsc --noEmit` (the `typecheck` stage of `bash scripts/check.sh`) fails
on master with
`tests/screen.test.ts(165,22): error TS2345: Argument of type 'string | undefined' is not assignable to parameter of type 'string'.`
because `t.writes[0]` is `string | undefined` under
`noUncheckedIndexedAccess`. Make the file type-check without weakening
the test.

## Context
- Line ~165: `expect(stripAnsi(t.writes[0])).toBe('abcdefghijklRRRRRR');`.
  Use a checked access (`const first = t.writes[0]; expect(first).toBeDefined();
  expect(stripAnsi(first ?? '')).toBe(...)`) or a non-null assertion — either
  is fine. Grep the file for any other `writes[0]` passed to `stripAnsi`.

## Acceptance criteria
- [ ] `npx tsc --noEmit` exits 0 and `bash scripts/check.sh` passes the
      `typecheck` and `unit` stages.
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/screen.test.ts` exits 0
      (the test still asserts the same string).
- [ ] No files changed outside scope

## Out of scope
Everything else.

## Worker report
