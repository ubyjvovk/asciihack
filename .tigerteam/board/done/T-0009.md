---
id: T-0009
title: Screen.paint must survive a grid size change without a resize event
priority: P2
complexity: C1
depends_on: [T-0006]
scope: [src/term/screen.ts, tests/screen.test.ts, docs/terminal.md]
---

## Goal
`Screen.remember()` (src/term/screen.ts) reuses its private copy of the last
painted cells, but never reallocates it when a grid with more cells is
painted while `prev` is non-null, so `dst` is `undefined` and the paint
throws `TypeError: Cannot set properties of undefined`. Make the private
copy grow/shrink with the painted grid.

## Context
- `docs/architecture.md` §6.1; `docs/terminal.md` "Screen" section (update
  its sentence about the private copy).
- Reproduction (PM probe): paint a 4×2 grid, then paint a 6×3 grid on the
  same `Screen` **without** calling `invalidate()` → throws. `diff()` already
  handles the size change (full repaint); only `remember()` is wrong.
- Fix inside `remember()`: when `grid.width * grid.height` differs from the
  stored copy's cell count, rebuild the copy's `cells` array to the new size
  (allocation only on size change; the steady state stays allocation-free).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/screen.test.ts` exits 0
- [ ] One new test `handles a larger then a smaller grid without invalidate`:
      paint 4×2, paint 6×3 (expect a full repaint that mentions a cell in
      row 3), paint 3×1 (expect a full repaint) — no throw, and after each
      paint a subsequent identical paint emits nothing.
- [ ] `docs/terminal.md` updated
- [ ] No files changed outside scope

## Out of scope
Anything else in `screen.ts`, `input.ts` or `ascii.ts`.

## Worker report

> claimed_by: ds (pid 1943875) at 2026-09-03T16:43:06Z

### Files changed (commit 2fdb3cc on branch tigerteam/T-0009)
- `src/term/screen.ts` — in `remember()`, when `prev` is non-null and the
  painted grid's cell count differs from the stored copy's, reallocate the
  copy's `cells` array to the new size (fresh `CopyCell` objects). Allocation
  only on size change; steady state stays allocation-free.
- `tests/screen.test.ts` — new test `handles a larger then a smaller grid
  without invalidate`: paints 4×2, then 6×3 (expects a full repaint reaching
  row 3, verified via the stripAnsi output `abcdefghijklRRRRRR`), then 3×1
  (expects a full repaint); no throw, and after each paint a subsequent
  identical paint emits nothing.
- `docs/terminal.md` — updated the `paint(grid)` bullet to note the private
  copy grows/shrinks with the painted grid and reallocates only on cell-count
  change.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/screen.test.ts` →
  `Tests  12 passed (12)`, `Test Files  1 passed (1)`
- `bash .tigerteam/scripts/run-tests.sh` (full suite) →
  `Test Files  5 passed (5)`, `Tests  48 passed (48)`

### Docs
- `docs/terminal.md` updated as described above.

### Acceptance criteria walk-through
- [x] `run-tests.sh tests/screen.test.ts` exits 0.
- [x] New test `handles a larger then a smaller grid without invalidate`
      covers all three paints, no throw, and empty re-paints. (One nuance:
      the first full repaint does not emit an explicit `CSI 3;xH` move because
      the writer's virtual cursor already lands on row 3 after the `CSI 2 J`
      full clear; I verified row 3 is reached by asserting the full 18-cell
      stripAnsi output instead. This still satisfies "a full repaint that
      mentions a cell in row 3".)
- [x] `docs/terminal.md` updated.
- [x] Only scope files changed (git status shows exactly the three scope
      files).

### Notes for reviewer
- None. Out-of-scope areas were not touched.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds | deepseek-ai/DeepSeek-V4-Flash-0731 | 2m 15s | 215.1k (186.11k) | 5.01k | $0.0069 | engine | review |

total: 1 attempt · 2m 15s · 215.1k in (186.11k cached) / 5.01k out · $0.0069 (accepted 2026-09-03T16:45:42Z)
