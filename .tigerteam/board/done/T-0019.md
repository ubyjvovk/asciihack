---
id: T-0019
title: Overlay boxes need an opaque panel background
priority: P1
complexity: C1
depends_on: [T-0018]
assignee: muse
scope: [src/ui/grid.ts, src/ui/overlays.ts, tests/ui-overlays.test.ts, docs/ui.md]
---

## Goal
Menus, text windows and prompts are unreadable over the 3D viewport: the
scene shows through the box. `paintBox` (src/ui/grid.ts) draws only the
border and the title; interior cells keep whatever the scene painted, and
short lines leave the rest of the row showing the scene. Give every
overlay box an opaque panel.

## Context
- `src/ui/grid.ts` `paintBox` / `putText` / `UI_BG`; every overlay in
  `src/ui/overlays.ts` calls `paintBox` then `putText` per line.
- Fix in `paintBox`: before drawing the border, fill **every** cell of the
  `w × h` box with `' '`, fg `UI_FG`, bg `PANEL_BG` (new export in
  `grid.ts`: `[18, 18, 26]` — a hair lighter than the black scene so the
  panel is visible against a dark background too); draw the border and
  title on the same bg; make the returned `inner` rect's default text bg
  `PANEL_BG` (give `putText` callers in overlays that bg — simplest: add
  an optional `bg` to the overlays' text calls, or a `putPanelText`
  helper in grid.ts that uses `PANEL_BG`). Also pad: clear one extra
  column on each side and one extra row above/below the box (a margin, in
  `UI_BG` black) so text never touches the scene glyphs.
- Do not touch the message line / `--More--` painter (it already uses a
  solid `UI_BG`).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/ui-overlays.test.ts` exits 0
- [ ] One new test, `overlay box clears its interior and margin`: fill a
      grid with `#` on a red bg, paint a menu overlay, then assert every
      cell inside the box (border included) has bg `PANEL_BG` and no `#`,
      and the one-cell margin around it is `' '` on `UI_BG`.
- [ ] Manual check (report): open the inventory (`i`) in fps mode with the
      bridge built — the panel is fully opaque.
- [ ] `docs/ui.md` overlay section: one sentence on the panel background.
- [ ] No files changed outside scope

## Out of scope
Anything else in the overlays (paging, keys) or the renderers.

## Worker report

> claimed_by: muse (pid 2044552) at 2026-09-03T20:32:04Z

- files changed and why (all in scope):
  - `src/ui/grid.ts` — new `PANEL_BG=[18,18,26]` export + `putPanelText` helper; `paintBox` now clears a one-cell `UI_BG` margin then fills the whole `w×h` box with `' '` on `PANEL_BG` before drawing border/title on `PANEL_BG`.
  - `src/ui/overlays.ts` — all boxed overlays (menu/text/yn/getlin/extcmd/message-menu) use `putPanelText` so content bg is `PANEL_BG`; `MoreOverlay` untouched (already solid `UI_BG`).
  - `tests/ui-overlays.test.ts` — new test `overlay panel background / overlay box clears its interior and margin`.
  - `docs/ui.md` — overlay section notes the opaque `PANEL_BG` panel + `UI_BG` margin.
- commands run:
  - `bash .tigerteam/scripts/run-tests.sh tests/ui-overlays.test.ts` → exit 0, 14 passed (log `.tigerteam/logs/tests/20260903-203351-156.log`).
- acceptance criteria:
  - run-tests exits 0: done (14/14 pass).
  - new test `overlay box clears its interior and margin`: done — fills grid with `#` on red, paints a menu overlay, asserts every box cell is `PANEL_BG` with no `#` and the margin is `' '` on `UI_BG`.
  - manual fps inventory check with bridge: NOT run — bridge not built in this worktree and I cannot view rendered frames; opacity is verified via the grid-cell assertions above, PM to eyeball.
  - docs/ui.md sentence: done.
  - no files outside scope: `git status` shows only the 4 scope files; commit `4fc5f2f` on branch `tigerteam/T-0019`.
- reviewer notes: while editing I briefly dropped a `handleKey` line in `YnOverlay` (multi-edit collision); restored and tests pass. `paintBox` margin/panel loops clip to grid bounds so edge-clamped boxes are safe.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | muse | meta/muse-spark-1.3-contributor | 2m 5s | 926.18k (694.92k) | 14.67k | $0.03 | engine | review |

total: 1 attempt · 2m 5s · 926.18k in (694.92k cached) / 14.67k out · $0.03 (accepted 2026-09-03T20:35:47Z)
