---
id: T-0012
title: nh-bridge — mark windows initialised so pline uses the message window
priority: P1
complexity: C1
depends_on: [T-0010]
scope: [bridge/nh-bridge.c, scripts/bridge-smoke.ts, tests/bridge.test.ts, docs/bridge.md]
---

## Goal
NetHack's `pline()` (`nethack/src/pline.c` ~line 239) sends every message
through `raw_print()` while `iflags.window_inited` is false; every real
window port sets that flag to TRUE at the end of its `init_nhwindows`
(`win/tty/wintty.c` ~1885, `win/curses/cursmain.c` ~493), but the shim
port never does, so today all game messages bypass the message window
(no message-window `putstr`, no history, no forced `--More--`). Set the
flag in the bridge right after forwarding `init_nhwindows`.

## Context
- `bridge/nh-bridge.c`, the `shim_init_nhwindows` branch of
  `handle_callback`: after `emit_call_end_noreply()`, add
  `iflags.window_inited = TRUE;` (`iflags` is the global from `hack.h`,
  already available — the bridge already uses `svp.plname`).
- Verify with `bash scripts/nethack-build.sh bridge` and
  `npx tsx scripts/bridge-smoke.ts`. After the change the welcome message
  ("… welcome to NetHack!  You are a …") must arrive as a `putstr` call
  on the message window (the window id the client returned for
  `create_nhwindow(NHW_MESSAGE)`), not as `raw_print`.

## Acceptance criteria
- [ ] `bash scripts/nethack-build.sh bridge` succeeds (`-Wall -Wextra -Werror`).
- [ ] `scripts/bridge-smoke.ts` additionally asserts that before the first
      key request a `putstr` on the message window whose text contains
      `welcome to NetHack` was seen, and prints `welcome via putstr: ok`;
      `tests/bridge.test.ts` asserts the same (still `describe.skipIf` when
      the bridge is not built). `npx tsx scripts/bridge-smoke.ts` exits 0;
      `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] `docs/bridge.md`: a short "Messages" paragraph (window_inited, what
      changed, that `raw_print` still appears for early/late text).
- [ ] No files changed outside scope

## Out of scope
Anything in `src/engine/` (T-0003 handles both message paths) and any
other bridge change.

## Worker report

> claimed_by: ds-1 (pid 1910807) at 2026-09-03T17:21:37Z

### Summary

- **bridge/nh-bridge.c** — in the `shim_init_nhwindows` branch of
  `handle_callback`, added `iflags.window_inited = TRUE;` right after
  `emit_call_end_noreply()`, with a comment citing `win/tty/wintty.c ~1885`.
  Verified `window_inited` is a `boolean` member of `iflags`
  (`nethack/include/flag.h:303`) and that `pline.c` routes through
  `raw_print()` only while it is false (`nethack/src/pline.c:239`).
- **scripts/bridge-smoke.ts** — track the message-window id (the id the client
  returns for `create_nhwindow(NHW_MESSAGE)`), watch `putstr` calls, and before
  the first key request assert a `putstr` on that window whose text contains
  `welcome to NetHack` was seen; prints `welcome via putstr: ok`.
- **tests/bridge.test.ts** — same assertion in the first vitest test (still
  under `describe.skipIf(!bridgeReady)`); also asserts `msgWinId` was set.
- **docs/bridge.md** — new `## Messages` paragraph: `window_inited`, what
  changed, and that `raw_print` still appears for early/late text (and
  recursive `pline()`), with the `raw_printed` counter reference.

### Commands run

- `bash scripts/nethack-src.sh` — populated the empty nethack submodule
  (needed for verification + build).
- `bash scripts/nethack-build.sh bridge` — succeeded; compiled `nh-bridge`
  with `-Wall -Wextra -Werror`, reply tests 10/10.
- `npx tsx scripts/bridge-smoke.ts` — exit 0; printed
  `hero at (69, 12), 63 map cells, 63 print_glyph calls` then
  `welcome via putstr: ok`.
- `bash .tigerteam/scripts/run-tests.sh` (full) — `Test Files 8 passed (8)`,
  `Tests 70 passed (70)`, exit 0. (bridge.test.ts: 3/3 passed.)
- `bash scripts/check.sh` — **FAILS at the typecheck stage** on a
  pre-existing error in `tests/screen.test.ts(165,22)` (TS2345). I confirmed
  this is pre-existing by `git stash`ing my changes and rerunning
  `npx tsc --noEmit` — the same error appears with a clean tree. It is
  outside this ticket's scope (`tests/screen.test.ts` is not in scope) so I
  did not touch it.

### Docs

`docs/bridge.md` — added the `## Messages` paragraph (acceptance criterion
met).

### Commit

- `0e1f07f` on branch `tigerteam/T-0012` — `[T-0012] nh-bridge: mark windows
  initialised so pline uses the message window` (4 scope files only).

### For the reviewer

- All four acceptance criteria verified runnable: build, smoke, vitest, docs,
  scope. The only gate failure is the pre-existing `screen.test.ts` typecheck
  error noted above — unrelated to this ticket.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 5m 17s | 837.04k (713.73k) | 9.06k | $0.03 | engine | review |

total: 1 attempt · 5m 17s · 837.04k in (713.73k cached) / 9.06k out · $0.03 (accepted 2026-09-03T17:28:27Z)
