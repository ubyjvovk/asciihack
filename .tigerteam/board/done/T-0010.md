---
id: T-0010
title: nh-bridge hardening — vararg widths, fallback return slot, select_menu bounds
priority: P2
complexity: C1
depends_on: [T-0002]
scope: [bridge/nh-bridge.c, docs/bridge.md]
---

## Goal
Three small correctness fixes in `bridge/nh-bridge.c` found in PM review
(listed in `docs/architecture.md` §3.3 "hardening backlog"). No behaviour
change on x86-64 today; they remove undefined behaviour and an
out-of-bounds read.

## Context
- `bridge/nh-bridge.c`, function `handle_callback`. The shim's argument
  formats are in `nethack/win/shim/winshim.c` (`DECLCB`/`VDECLCB` lines).
- Build and test: `bash scripts/nethack-build.sh bridge` (compiles with
  `-Wall -Wextra -Werror`, runs `make -C bridge test`), then
  `npx tsx scripts/bridge-smoke.ts` must still print the hero line.

## Acceptance criteria
- [ ] `shim_start_menu`: `mbehavior` is declared `unsigned long` in the
      shim; read it with `va_arg(ap, unsigned long)` (not `int`) and print
      it with `%lu`. Fix the misleading comment.
- [ ] Unknown-call fallback (bottom of `handle_callback`): never
      `memset(ret_ptr, 0, sizeof(void *))` — the return slot may be a 1-byte
      `char`/`boolean` or a 2-byte `short`. Zero exactly the width named
      by `fmt[0]`: `c`/`b`/`0` → 1 byte, `2` → `short`, `i` → `int`,
      `s`/`p` → pointer, `v` → nothing.
- [ ] `shim_select_menu`: guard `w < 0 || w >= MAX_WIN` before touching
      `g_menus[w]` (treat as "no items": `ret = 0`, `*menu_list = NULL`).
- [ ] `bash scripts/nethack-build.sh bridge` succeeds and
      `npx tsx scripts/bridge-smoke.ts` exits 0;
      `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] `docs/bridge.md`: remove the corresponding lines from its known-limits
      list if present; one sentence noting the fallback zeroes by `fmt`.
- [ ] No files changed outside scope

## Out of scope
Any protocol change, any new call handling, anything in `reply.c`.

## Worker report

**Files changed**
- `bridge/nh-bridge.c` — the three hardening fixes:
  - `shim_start_menu`: `mbehavior` now read with `va_arg(ap, unsigned long)`
    and printed with `%lu`; corrected the misleading comment that claimed it
    arrives as `int`.
  - Unknown-call fallback: no longer `memset(ret_ptr, 0, sizeof(void *))`;
    zeroes exactly the width named by `fmt[0]` (`c`/`b`/`0` → 1 byte,
    `2` → `short`, `i` → `int`, `s`/`p` → pointer, `v` → nothing). Also
    removed the now-unused `(void) fmt;` / stale comment since `fmt` is used.
  - `shim_select_menu`: added `w >= 0 && w < MAX_WIN` guard before indexing
    `g_menus[w]`; an out-of-range id yields no items (`ret` 0,
    `*menu_list` NULL).
- `docs/bridge.md` — added one sentence noting the unknown-call fallback
  zeroes by `fmt` width (the hardening items had no known-limits lines in this
  file to remove).

**Commands run / results**
- `bash scripts/nethack-build.sh bridge` — compiled `nh-bridge.c` with
  `-Wall -Wextra -Werror` (no warnings), reply_test `10 passed, 0 failed`.
- `npx tsx scripts/bridge-smoke.ts` — exits 0; prints
  `hero at (77, 3), 35 map cells, 35 print_glyph calls`.
- `bash .tigerteam/scripts/run-tests.sh` — `Test Files 7 passed (7)`,
  `Tests 63 passed (63)`; `tests/bridge.test.ts` (3 tests) passed against the
  built bridge.

**Doc changes** — `docs/bridge.md` (see above). The `docs/architecture.md`
§3.3 hardening-backlog line is now stale but is out of this ticket's scope;
left for the PM to reconcile.

**Commit** — `c7e3b52` on branch `tigerteam/T-0010`.

**For the reviewer** — All three fixes are compile-verified; the fallback's
`fmt[0]` switch and the `select_menu` bounds guard are only reachable on
paths not exercised by the smoke/test flow, so they are verified by build
(`-Werror`) and code inspection rather than runtime. No out-of-scope changes
made; build artifacts are gitignored.

> claimed_by: ds-1 (pid 1910807) at 2026-09-03T17:02:24Z
> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 3m 48s | 759.87k (711.94k) | 8.81k | $0.02 | engine | review |

total: 1 attempt · 3m 48s · 759.87k in (711.94k cached) / 8.81k out · $0.02 (accepted 2026-09-03T17:07:51Z)
