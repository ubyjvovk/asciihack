---
id: T-0034
title: Lit and dark rooms — remember lighting per cell and render unlit floor dimmer
priority: P2
complexity: C2
depends_on: [T-0033]
scope: [src/engine/session.ts, src/engine/glyphs.ts, src/render/raycast.ts, src/render/ortho.ts, tests/engine-session.test.ts, tests/engine-glyphs.test.ts, tests/raycast.test.ts, tests/ortho.test.ts, tests/raycast-textured-golden.txt, tests/ortho-golden.txt, tests/ortho-zoom-golden.txt, docs/engine.md, docs/render.md]
---

## Goal
NetHack distinguishes lit room floor (`S_room`) from remembered dark floor
(`S_darkroom`) and lit corridors (`S_litcorr`) from dark ones (`S_corr`).
Carry that into the model and the two 3D renderers so a dark room reads
as dark (you remember its shape, not its light) and a lit one as lit —
a real gameplay cue for free.

## Context
- `src/model/types.ts` (PM-owned): `MapCell.lit?: boolean` exists (added
  by the PM). `src/engine/glyphs.ts` (`classifyCmap`), `src/engine/session.ts`
  (`print_glyph` handling, §4.2 rules), `docs/render.md` (levels).
- Engine: when a `cmap` glyph sets a cell's terrain, also set `lit`:
  `S_room`, `S_litcorr` → `true`; `S_darkroom`, `S_corr` → `false`; other
  kinds leave it `undefined`. A monster/object glyph on a cell does not
  change `lit`. Export `litOf(symIdx, S): boolean | undefined` from
  `glyphs.ts` and use it in the session.
- Raycaster (`src/render/raycast.ts` floor pass): floor cells with
  `lit === false` use the base colour ×0.45 and no flagstone highlight
  (stones still vary but darker); corridors already dark stay as they
  are unless `lit === true` (then ×1.4). Walls are unaffected (they are
  lit by the hero's lantern in the fiction). Grid lines keep their
  absolute level, so dark rooms still show perspective.
- Ortho (`src/render/ortho.ts` floor pass): same ×0.45 for unlit floor
  tiles.
- Fixture: extend `levelFromAscii` (tests/fixtures/levels.ts is not in
  scope — instead build the test level in the test with `cellAt`-style
  overrides, or add a `lit` map parameter to your own helper inside the
  test file) so a test can mark a region dark.
- Regenerate the goldens only if they change (the `ROOM` fixture has no
  `lit` info, so they should not).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/engine-session.test.ts tests/engine-glyphs.test.ts tests/raycast.test.ts tests/ortho.test.ts` exits 0; full suite green.
- [ ] Tests, by name (only these): `litOf` for the four symbols and an
      unrelated one; a `print_glyph` with `S_darkroom` sets `lit: false`
      and a later monster glyph on that cell keeps it; a dark floor row 2
      cells away is darker than the same row in a lit room (raycaster);
      the same for an ortho floor tile.
- [ ] `docs/engine.md` (the `lit` rule) and `docs/render.md` (the
      levels) updated.
- [ ] No files changed outside scope

## Out of scope
Wall shading by room light, the browser scene (T-0031's builder can read
`lit` later), the minimap.

## Worker report

> claimed_by: opus-1 (pid 2044552) at 2026-09-04T01:08:46Z

### Files changed

- `src/engine/glyphs.ts` — new `litOf(symIdx, S)` classifier: true for
  `S_room`/`S_litcorr`, false for `S_darkroom`/`S_corr`, undefined otherwise.
- `src/engine/session.ts` — `doPrintGlyph` calls `litOf` for cmap terrain
  glyphs and writes `cell.lit` only when non-undefined (so trap/monster/object
  glyphs and unrelated cmap symbols leave the existing value alone).
  `resetMap` clears `cell.lit` alongside the other per-cell fields.
- `src/render/raycast.ts` — floor pass reads `level.cellAt(mx, my)?.lit`;
  `floor` stone bodies dim ×0.45 when `lit === false`, `corridor` bodies
  brighten ×1.4 when `lit === true`. Grid lines, door posts and the absolute
  edges are untouched so perspective still reads in a dark room.
- `src/render/ortho.ts` — floor pass dims `kind === 'floor'` stone bodies
  ×0.45 when the cell's `lit === false`; seams keep their absolute `0.30`.
- `tests/engine-glyphs.test.ts` — one `litOf` test covering the four named
  symbols and `S_stone` as the unrelated case.
- `tests/engine-session.test.ts` — added `S_darkroom` to `makeHello` and a
  test that a print_glyph with `S_darkroom` sets `lit: false` and a later
  monster glyph on that cell keeps it.
- `tests/raycast.test.ts` — dark-room floor row 2 cells away compared vs
  the same row in a lit room; the ratio of stone-body peaks is ~0.45.
- `tests/ortho.test.ts` — same comparison on the hero's ortho floor tile,
  with a seam-brightness check to confirm seams stay at absolute 0.30.
- `docs/engine.md` — new "Cell lighting (`MapCell.lit`)" section and `lit`
  called out on the `session.map` bullet.
- `docs/render.md` — new "Lighting (`MapCell.lit`)" subsection on the
  raycaster's Floor, and a lit note in the ortho "Look" section.

### Commands run

```
bash .tigerteam/scripts/run-tests.sh tests/engine-session.test.ts \
     tests/engine-glyphs.test.ts tests/raycast.test.ts tests/ortho.test.ts
Test Files  4 passed (4)
     Tests  81 passed (81)

bash .tigerteam/scripts/run-tests.sh   # full suite
Test Files  23 passed | 1 skipped (24)
     Tests  240 passed | 3 skipped (243)

npx tsc --noEmit   # clean, per STATE.md review checklist item 0
```

The `raycast/performance` benchmark ran at 1.441 ms average (200×60 with three
tiled sprites) — well under the 8 ms budget; the extra `level.cellAt(...)?.lit`
per floor cell did not measurably move it. Same for the ortho 200×60 pass.

### Doc changes

- `docs/engine.md` — "Cell lighting (`MapCell.lit`)" section explaining how
  the session updates `lit`, and `lit` added to the `session.map` bullet.
- `docs/render.md` — the raycaster's Floor section gained a "Lighting"
  subsection (×0.45 dim, ×1.4 corridor boost, grid lines untouched), and the
  ortho "Look" section notes the ×0.45 floor dim with seams unchanged.

### Commit

- `4accaf2` on branch `tigerteam/T-0034` (isolated checkout).

### Notes for the reviewer

- The `lit` update path is deliberately narrow: only cmap terrain glyphs
  where `litOf` returns a concrete boolean write to `cell.lit`. Trap glyphs,
  monster/object glyphs, and unrelated cmap symbols (walls, doors, stairs,
  fountains, …) all leave the previously stored value alone — matches the
  ticket's "other kinds leave it undefined" and "monster/object glyph on a
  cell does not change lit". `clear_nhwindow(WIN_MAP)` clears `lit` back to
  `undefined` alongside the other per-cell fields (new one-liner in
  `resetMap`, in scope).
- The raycaster only touches the floor pass; walls, ceilings, sprites, the
  unknown veil and the flat (`detail: false`) path are unchanged. The
  `detail: false` and default-detail goldens still match byte-for-byte
  (the `ROOM` fixture has no `lit` info, so `lit` reads as undefined).
- Ortho only touches `kind === 'floor'` inside the non-seam branch of the
  floor pass. Doorways/open doors and unexplored are not dimmed. All three
  ortho goldens (`ortho-golden.txt`, `ortho-zoom-golden.txt`) still match.
- One out-of-scope observation, not touched: the raycaster now calls
  `level.cellAt(mx, my)` per floor cell, one extra function call inside a
  hot loop. Fine at current sizes (see the 200×60 timing above); if a
  future perf pass wants to squeeze it, hoisting the `lit` lookup outside
  the per-column loop when the row's cell doesn't change would help.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-4-7 | 9m 39s | 9.58M (9.58M) | 42.09k | $7.43 | engine | review |

total: 1 attempt · 9m 39s · 9.58M in (9.58M cached) / 42.09k out · $7.43 (accepted 2026-09-04T01:30:04Z)
