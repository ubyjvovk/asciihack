---
id: T-0005
title: First-person raycaster over the level grid (pure, golden-tested)
priority: P1
complexity: C2
depends_on: []
scope: [src/render/raycast.ts, tests/raycast.test.ts, tests/fixtures/levels.ts, tests/raycast-golden.txt, docs/render.md]
---

## Goal
`renderFirstPerson(level, pose, sprites, fb, opts)` — the Wolfenstein-style
grid raycaster that turns the remembered NetHack map into a first-person
scene in a `FrameBuffer`, exactly as `docs/architecture.md` §5.2 specifies.
Pure and deterministic so it can be golden-tested in node.

## Context
- Read `docs/architecture.md` §5.1, §5.2, §7 (coordinates: `x` east,
  `y` south, yaw 0 = north, +π/2 = east clockwise) and §10. Types are
  PM-owned in `src/model/types.ts`: `LevelView`, `Pose`, `Sprite`,
  `FrameBuffer`, `makeFrameBuffer`, `isSolid`, `CellKind`.
- `tests/fixtures/levels.ts`: `levelFromAscii(rows: string[]): LevelView`
  building a `LevelView` from a legend (`#` wall, `.` floor, `+` closed
  door, `|`/`-` also wall, `'` open door, `D` doorway, `~` water, `T` tree,
  `>`/`<` stairs, space = unexplored, `%` corridor). Export a `ROOM` (a
  12×8 room with a door on the east wall and a corridor beyond) and an
  `L_SHAPED` level for tests.
- `RaycastOptions {fovDeg?: number (70), maxDepth?: number (24),
  cellAspect?: number (2 — cell height / width), fogK?: number (0.18)}`.
- Colours by kind (linear RGB 0..1, before exposure): wall `[0.55, 0.53,
  0.50]`, door_closed `[0.55, 0.35, 0.15]`, door_open/doorway frame `[0.45,
  0.30, 0.12]`, tree `[0.15, 0.45, 0.15]`, bars `[0.3, 0.6, 0.65]`,
  stone/unexplored `[0.06, 0.06, 0.07]`; floor `[0.40, 0.37, 0.33]`,
  corridor `[0.28, 0.22, 0.16]`, water `[0.10, 0.25, 0.60]`, lava `[0.85,
  0.35, 0.05]`, ice `[0.55, 0.75, 0.85]`, stairs_up/stairs_down `[0.85,
  0.85, 0.40]`, altar `[0.7, 0.7, 0.75]`, fountain `[0.3, 0.5, 0.9]`,
  trap `[0.6, 0.2, 0.6]`, anything else floor-coloured. Ceiling: `[0.03,
  0.03, 0.04]` fading to black at the top row. Export the table as
  `KIND_COLORS` so the ortho renderer (later) reuses it.
- Depth plane: perpendicular wall distance for wall cells, floor-row
  distance for floor/ceiling cells; `Infinity` never appears inside
  `maxDepth` except where the ray leaves the map.
- Sprites: sort far to near, project, clip to the wall depth per column,
  fill `overlayCh`/`overlayRgb` in the rectangle (§5.2); a sprite exactly
  on the camera cell is skipped.
- Golden test: render `ROOM` from the centre facing east at 80×24 into a
  buffer, quantize it with a *test-local* 10-glyph ramp (`" .:-=+*#%@"`
  over `max(r,g,b)` — do **not** import `src/render/ascii.ts`, it is being
  written in parallel) and compare against `tests/raycast-golden.txt`
  (commit the golden; print a diff on failure). Regenerate with
  `UPDATE_GOLDEN=1`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/raycast.test.ts` exits 0
- [ ] New tests cover: a wall straight ahead at distance 3 lands at the
      expected screen height (±1 row) and column range; facing a wall at
      45° gives the E/W face shade (70 %) on one side; `unexplored` stops
      rays; a closed door is solid and door-coloured, an open door is not
      solid; the floor row under a `water` cell is blue-dominant; a sprite
      behind a wall is not drawn, a sprite in view writes its `ch` into
      `overlayCh` with more cells when nearer; yaw 0 looks north (the
      north wall of `ROOM` fills the view), +π/2 looks east; two renders
      with the same input are byte-identical; the golden matches;
      rendering 200×60 takes under 8 ms on average over 20 runs (log the
      number, assert < 40 ms so CI noise never fails it).
- [ ] Every exported function has a JSDoc line; `docs/render.md` describes
      the algorithm, the colour table, the aspect correction, and the
      golden workflow.
- [ ] No files changed outside scope

## Out of scope
- The ASCII quantizer (T-0006) — use only the test-local ramp.
- Ortho rendering, textures, lighting from NetHack's lit/dark rooms
  (later tickets).
- Any I/O.

## Worker report

> claimed_by: ds-2 (pid 1910807) at 2026-09-03T16:04:40Z

**Files changed (all within scope):**
- `src/render/raycast.ts` — `renderFirstPerson` (grid-DDA walls, aspect-corrected
  vertical FOV, 70 %/100 % face shading, ceiling gradient, floor casting, fog,
  depth-tested billboard sprites), plus exported `KIND_COLORS` and `CEILING_COLOR`.
- `tests/raycast.test.ts` — 12 tests covering every acceptance criterion, plus a
  test-local 10-glyph quantizer (T-0006 ascii.ts deliberately not imported) and
  UPDATE_GOLDEN support.
- `tests/fixtures/levels.ts` — `levelFromAscii`, `ROOM`, `L_SHAPED`.
- `tests/raycast-golden.txt` — committed golden of ROOM at 80×24 facing east.
- `docs/render.md` — algorithm, colour table, aspect correction, golden workflow.

**Commands run:**
- `bash .tigerteam/scripts/run-tests.sh tests/raycast.test.ts` → `Test Files 1 passed
  (1), Tests 12 passed (12)`, exit 0.
- `bash .tigerteam/scripts/run-tests.sh` (full suite) → `Test Files 2 passed (2),
  Tests 15 passed (15)`.
- `npx tsc --noEmit` → clean.
- Perf log line: `raycast 200×60 average: 1.224 ms over 20 runs` (assert < 40 ms).
- Golden generated once with `UPDATE_GOLDEN=1`, then re-verified without it.

**Docs:** wrote `docs/render.md`; every exported function/const has a JSDoc line.

**Commit:** `a597f89` on branch `tigerteam/T-0005`.

**Notes for the reviewer:**
- Doorways (open door / doorway) are passable and their floor is tinted in the
  door colour; the spec's literal "thin frame" cannot be drawn because `CellKind`
  carries no door orientation (S_ndoor vs S_vodoor/S_hodoor). I approximated it
  with the door-coloured floor and documented this in docs/render.md — flag if the
  frame is required.
- Shading test uses a hue gate to separate wall pixels from floor pixels; the
  goldens were eyeballed from the ASCII dump (rows 7–14 show the east wall with
  a central doorway gap, floor below).
- The `renderScene` convenience helper was added then removed to keep scope tight.
> handoff: review

## Rework (attempt 2)

Good work overall (clean DDA, 0.35 ms at 200×60, golden and docs fine). Two
correctness bugs found by the PM, both reproduced with a small probe:

1. **Stale overlay glyphs.** `renderFirstPerson` never resets `fb.overlayCh`
   (or `fb.overlayRgb`). Rendering a frame with one sprite and then the same
   pose with no sprites into the same buffer leaves all 165 sprite cells
   set. The real loop reuses one `FrameBuffer` per frame, so ghosts of every
   monster would persist forever. Fix: at the start of the function clear
   `overlayCh` (`fill(0)`) — every cell's `rgb` and `depth` must also be
   written every frame (see 2).

2. **Unpainted horizon row for odd heights.** With `rows = 21` (the default
   viewport is `H − 3`), `horizon = 10.5`; where a ray hits nothing (leaves
   the map or exceeds `maxDepth`), the ceiling pass stops at
   `floor(wallTop) = 10` exclusive and the floor pass starts at
   `ceil(horizon) = 11`, so row 10 is never written (probe: 80 stale cells,
   all in row 10, for an open 79×21 floor with `maxDepth: 3`). Fix so that
   every cell of the buffer is written each frame regardless of `rows`
   parity and of whether the column's ray hit a wall (treat a miss as a wall
   of zero height at the horizon, or paint the gap rows black with depth
   `Infinity`).

Add tests, by name, in `tests/raycast.test.ts`:
- `clears overlay glyphs from a previous frame`: render with a sprite, then
  without, same buffer → zero non-zero `overlayCh` entries.
- `paints every cell for odd heights and escaping rays`: pre-fill `rgb` and
  `depth` with −1, render an all-floor 79×21 level at `{x:40, y:10.5,
  yaw:0}` with `maxDepth: 3` into an 80×21 buffer → no −1 remains in either
  plane (also run it for 80×24 and 33×15).

Regenerate the golden only if it changes (it should not for 80×24).
Done looks like: those two tests pass, the existing 12 still pass, report
lists the commands.


> claimed_by: ds-3 (pid 1910807) at 2026-09-03T16:31:03Z

## Rework (attempt 3 — ds-3)

**Files changed (all within scope):**
- `src/render/raycast.ts` — at the top of `renderFirstPerson`, pre-fill the
  whole buffer: `overlayCh.fill(0)` / `overlayRgb.fill(0)` (clears stale overlay
  glyphs from a previous frame) and `rgb.fill(0)` / `depth.fill(Infinity)`
  (guarantees every cell is written every frame, regardless of `rows` parity or
  whether a column's ray escaped the map / exceeded `maxDepth`). This fixes both
  reported bugs with one change; the wall/ceiling/floor passes then overwrite
  the covered cells, so the golden (80×24, even height) is unchanged.
- `tests/raycast.test.ts` — two new tests, by name:
  - `clears overlay glyphs from a previous frame` — render with a sprite, then
    the same pose with none into the same buffer → zero non-zero `overlayCh`.
  - `paints every cell for odd heights and escaping rays` — pre-fill `rgb` and
    `depth` with −1, render an all-floor 79×21 level at `{x:40, y:10.5, yaw:0}`
    with `maxDepth: 3` into 80×21, 80×24 and 33×15 buffers → no −1 remains in
    either plane.

**Commands run:**
- `bash .tigerteam/scripts/run-tests.sh tests/raycast.test.ts` → `Test Files 1
  passed (1), Tests 14 passed (14)`, exit 0.
- `bash .tigerteam/scripts/run-tests.sh` (full suite) → `Test Files 2 passed (2),
  Tests 17 passed (17)`.
- `npx tsc --noEmit` → clean.
- Perf log line: `raycast 200×60 average: 0.931 ms over 20 runs` (assert < 40 ms).
- Golden re-verified without `UPDATE_GOLDEN=1` — unchanged (git status shows no
  diff to `tests/raycast-golden.txt`).

**Docs:** no doc change needed — `docs/render.md` (written in attempt 2) already
  describes the algorithm, colour table, aspect correction and golden workflow;
  this rework only fixes correctness bugs and adds tests.

**Commit:** `5fc867e` on branch `tigerteam/T-0005` (on top of `a597f89`).

**Notes for the reviewer:**
- Both acceptance criteria from the rework are covered by the two new tests,
  which pass alongside the original 12.
- The buffer pre-fill is cheap (an O(n) memset-style fill per frame) and did not
  move the 200×60 perf number materially (~0.93 ms).

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-2 | deepseek-ai/DeepSeek-V4-Flash-0731 | 21m 50s | 2.28M (2.01M) | 63.03k | $0.07 | engine | review |
| 2 | ds-3 | deepseek-ai/DeepSeek-V4-Flash-0731 | 3m 33s | 512.1k (478.46k) | 9.26k | $0.01 | engine | review |

total: 2 attempts · 25m 23s · 2.79M in (2.48M cached) / 72.29k out · $0.09 (accepted 2026-09-03T16:36:11Z)
