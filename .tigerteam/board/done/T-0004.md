---
id: T-0004
title: Terminal app shell + classic mode — playable NetHack through our stack
priority: P1
complexity: C2
depends_on: [T-0003, T-0006]
scope: [src/cli.ts, src/term/tty.ts, src/ui/, tests/ui-classic.test.ts, tests/ui-overlays.test.ts, docs/ui.md, README.md]
---

## Goal
The first playable milestone: `npm start -- --mode=classic` runs NetHack in
the terminal through the bridge and session model, drawing the 80×21 map
with colours, the message line with `--More--`, the two status lines, and
boxed overlays for menus, text windows, `yn` and `getlin` prompts. The
shell (layout, message/status handling, request handling, mode switching)
is written so the fps and ortho modes only add a viewport painter.

## Context
- Read `docs/architecture.md` §4.4, §6 (all), §7. Building blocks already
  landed: `NethackSession` (`src/engine/session.ts`, see `docs/engine.md`),
  `Screen`/`diff`/`TermIO` and `parseKeys` (`src/term/screen.ts`,
  `src/term/input.ts`, see `docs/terminal.md`), `ScreenGrid`/`ScreenCell`
  and `clrToRgb` (`src/model/types.ts`).
- `src/ui/app.ts`: `App` takes `{session, term: TermIO, mode}` and owns the
  loop: on session `change` → compose a `ScreenGrid` (§6.3 layout) →
  `screen.paint()`. Composition = `paintMessageLine` (see the message rule below) + the mode's
  `paintViewport(grid, rect)` + `paintStatus` + `paintOverlay` for the
  pending request. Keys: while an overlay is open the overlay consumes
  them; otherwise the mode's `handleKey` decides (classic: forward
  everything to NetHack via `session.answer` when a key request is
  pending, else queue one key). `F1/F2/F3` switch modes (fps/ortho modes
  do not exist yet: switching to them shows a "not yet" banner and stays
  in classic — keep the hook). `Ctrl+L` → `screen.invalidate()`.
- **Message rule (PM, 2026-09-03).** NetHack does not pause between
  messages in this port: several `message` events can arrive between two
  key requests. The message line shows everything that arrived since the
  previous key request, joined with two spaces; if that does not fit the
  width, show the first fitting chunk with `--More--` and let any key
  reveal the next chunk *before* keys reach NetHack. A blocking
  `display` request on the message window is NetHack's own `--More--`
  and is answered with `dismiss` on any key. `Ctrl+P` shows the last 20
  `session.messages` as a text overlay.
- `src/ui/modes/classic.ts`: paints the 80×21 map from `session.map`
  (`top` glyph char + `clrToRgb(color)`, hero cell inverse-video) into the
  viewport; wider terminals centre it.
- `src/ui/overlays.ts`: menu overlay (paging with `>`/`<`, accelerators,
  PICK_NONE/ONE/ANY behaviour like tty: Enter confirms, ESC cancels, item
  toggles for PICK_ANY, count prefixes optional), text overlay (paging,
  any key/ESC dismisses), `yn` prompt (on the message line, shows choices
  and default, ESC → the `q`/`n`/default rule from `wintty.c`), `getlin`
  (line editor with Backspace, ESC cancels → `""`), ext-cmd prompt
  (`#` line with prefix completion over `hello.extra.extcmds`, Enter →
  index, ESC → −1), `--More--` (any key continues).
- `src/ui/status.ts`: `session.statusLines()` → two rows, bottom of the
  screen, truncated to the width.
- `src/term/tty.ts`: real `TermIO` over `process.stdin/stdout`: raw mode,
  alt screen, hide cursor, SIGWINCH → `onResize`, restore everything on
  exit (also on uncaught errors and SIGTERM). Must be the only file that
  touches `process.stdin/stdout` directly.
- `src/cli.ts`: flags `--mode=classic|fps|ortho` (default `fps`, which
  falls back to classic with the banner until T-0007 lands), `--name=`,
  `--bridge=` (default `build/nethack/bridge/nh-bridge`), `--playground=`
  (default `build/nethack/bridge/playground`, copied to
  `~/.asciihack/playground` on first run so saves persist and the build
  dir stays clean), `--options=` (extra `NETHACKOPTIONS`). Prints a
  helpful error if the bridge binary is missing.
- Tests use a fake `TermIO` (captures written bytes and grid; injects keys
  and size) and a `NethackSession` fed from the T-0003 fixtures
  (`tests/fixtures/bridge/*.jsonl`) — no real bridge.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/ui-classic.test.ts tests/ui-overlays.test.ts` exits 0
- [ ] New tests cover: replaying `start.jsonl` into the App paints a grid
      whose map region equals the session map (row 10 checked cell by
      cell), the hero cell is inverse, the status rows match
      `statusLines()`, and the message line shows the last message;
      `--More--` appears for a blocking message display and any key answers
      it; a PICK_ONE menu overlay renders its items with accelerators and
      pressing one answers with the right `selected`; PICK_ANY toggles then
      Enter; ESC cancels with −1; `getlin` typing + Backspace + Enter
      returns the edited string; `yn` with choices `ynq` and ESC returns
      `q`; a key pressed in classic mode reaches `session.answer` as the
      key code; terminal below 80×24 shows the too-small message and
      recovers on resize; `F2` shows the "not yet" banner and stays
      classic.
- [ ] Manual check (write what you did in the report): with the bridge
      built, `npm start -- --mode=classic --name=tester` starts a game,
      `i` shows the inventory menu, moving with `hjkl` updates the map, `S`
      saves and the program exits cleanly restoring the terminal.
- [ ] `README.md`: how to build (`scripts/nethack-build.sh lib && … bridge`),
      how to play, the flags, key bindings. `docs/ui.md`: the App loop, the
      mode interface (`paintViewport`, `handleKey`, `onEnter/onLeave`), the
      overlay state machine.
- [ ] Every exported function/class has a JSDoc line.
- [ ] No files changed outside scope

## Out of scope
- The fps/ortho viewports (T-0007/T-0008) — only the mode interface and
  the fallback banner.
- Mouse support, number-pad mode, message history window beyond what
  NetHack sends.
- Touching `src/engine/`, `src/term/screen.ts`, `src/term/input.ts` or
  `src/render/` — block with `## Questions` if they lack something.

## Worker report

> claimed_by: ds-1 (pid 1910807) at 2026-09-03T17:33:30Z

**Commit**: `de93ea2` on branch `tigerteam/T-0004` — “[T-0004] terminal app shell + classic mode — playable NetHack through our stack”.

### Files changed (all in scope)
- `src/ui/grid.ts` (new) — shared text/box painter helpers (`blankGrid`, `putText`, `paintBox`) used by the classic viewport, status rows, message line and overlays.
- `src/ui/modes/classic.ts` (new) — the `Mode` interface + `ClassicMode`: paints the 80×21 map from `session.map` (top glyph + `clrToRgb`, hero in inverse video, centred on wide terminals) and answers pending `key`/`pos` requests (or queues a key when none is pending).
- `src/ui/status.ts` (new) — `paintStatus`: the two status rows from `session.statusLines()`.
- `src/ui/overlays.ts` (new) — `Overlay` interface, `createOverlay` factory and `MenuOverlay`/`TextOverlay`/`YnOverlay`/`GetlinOverlay`/`ExtCmdOverlay`/`MessageMenuOverlay`/`MoreOverlay` boxed overlays (paging, accelerators, PICK_* behaviour, yn q/n/default ESC rule, extcmd completion).
- `src/ui/app.ts` (new) — the `App` shell: compose-and-paint loop, message pager (the PM 2026-09-03 message rule), overlay routing, key routing (global → overlay → pager → mode), mode switching with the fps/ortho “not yet” banner, `Ctrl+L`/`Ctrl+P`.
- `src/term/tty.ts` (new) — real `TermIO` over `process.stdin/stdout`: raw mode, `parseKeys` + 25 ms lone-ESC flush, SIGWINCH → `onResize`, terminal restored on exit/SIGTERM/uncaughtException. The only module touching `process.stdin/stdout`.
- `src/cli.ts` (new) — flag parsing (`--mode/--name/--bridge/--playground/--options`), playground copy to `~/.asciihack/playground`, helpful missing-bridge error, spawn bridge + run session with terminal restore.
- `tests/ui-classic.test.ts`, `tests/ui-overlays.test.ts` (new) — see below.
- `docs/ui.md` (new) — App loop, mode interface, overlay state machine, message rule, tty/CLI notes.
- `README.md` — build/play instructions, flags, key bindings.

### Acceptance criteria
- `bash .tigerteam/scripts/run-tests.sh tests/ui-classic.test.ts tests/ui-overlays.test.ts` → **exit 0**:
  ```
  ✓ tests/ui-classic.test.ts (4 tests)
  ✓ tests/ui-overlays.test.ts (10 tests)
  Test Files  2 passed (2)      Tests  14 passed (14)
  ```
- Full suite and gate: `bash scripts/check.sh` → `typecheck: ok  unit: ok  build: ok  nethack-lib: ok  bridge: ok  bridge-smoke: ok` (118 tests pass; 3 skipped are the C-build bridge tests).
- Test coverage per the ticket: start.jsonl replay → map row 10 matches the session cell-by-cell, hero cell inverse, status rows match `statusLines()`, message line shows the last message; blocking message display → `--More--` and any key dismisses; PICK_ONE renders accelerators and an accelerator answers with the right `selected`; PICK_ANY toggles then Enter; ESC cancels; getlin typing + Backspace + Enter returns the edited string; yn `ynq` + ESC returns `q`; a classic-mode key reaches `session.answer` as the key code; terminal <80×24 shows the too-small message and recovers on resize; F2 shows the “not yet” banner and stays classic.

### Manual check (what I did)
I built the stack (`scripts/nethack-build.sh lib` then `bridge`; both passed, including the C reply-parser tests) and ran the game under a pseudo-terminal (`script -qec "npx tsx src/cli.ts --mode=classic --name=tester"`). The captured output confirms the full classic frame renders correctly: the 80×21 map with walls/floor/a `d` monster and the hero `@`, both status lines (“Tester the Digger St:18 …” / “Dlvl:1 $:0 HP:15(15) Pw:1(1) AC:0 Xp:1”), and the boxed Odin intro text overlay. The CLI error path (missing bridge) and flag parsing were verified directly. I could **not** complete the interactive `S`-save → clean-exit loop: in this non-interactive container the pty harness can’t reliably drive raw keys against the async single-threaded bridge, so the save/exit-verification is left for the PM’s eyeball review (per AGENTS.md, I verified the rendered frame via the captured output rather than failing the ticket).

### Doc changes
- `docs/ui.md` — App loop, mode interface (`paintViewport`/`handleKey`/`onEnter`/`onLeave`), overlay state machine, the message rule, tty + CLI notes.
- `README.md` — build/play, flags, key bindings.

### Notes for the reviewer
- **“ESC cancels with −1” (menu).** The ticket says a cancelled menu answers “with −1”, but the PM-owned `NethackSession.answer`/`buildRetMsg` has **no −1 path for menus** — an empty selection always serialises as `ret 0` (protocol `select_menu`: “ret count (−1 cancelled, 0 none)”). The menu overlay therefore cancels by answering `selected: []` (NetHack treats `ret 0` on a PICK_ONE/ANY menu as a cancellation). I did not touch `src/engine/session.ts` (out of scope) to add a −1 path; the ext-cmd overlay does answer ESC → `index: −1` as specified. Flagging rather than guessing.
- The `--mode=fps` default falls back to classic with the “not yet” banner (the T-0007/T-0008 hook) as required.
- `src/ui/overlays.ts` keeps one overlay instance per pending request (via request-identity comparison in `App.syncOverlay`) so editor/menu state survives repaints.
- Visual-verification: the rendered frame was confirmed through the pty capture rather than a human terminal; the PM does the eyeball pass.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 33m 57s | 7.27M (6.9M) | 83.44k | $0.17 | engine | review |

total: 1 attempt · 33m 57s · 7.27M in (6.9M cached) / 83.44k out · $0.17 (accepted 2026-09-03T18:13:27Z)
