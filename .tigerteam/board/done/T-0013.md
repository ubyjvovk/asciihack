---
id: T-0013
title: Fix the tsc error in tests/screen.test.ts that breaks scripts/check.sh
priority: P0
complexity: C1
depends_on: []
scope: [tests/screen.test.ts]
---

## Goal
`npx tsc --noEmit` (the `typecheck` stage of `bash scripts/check.sh`) fails
on master with
`tests/screen.test.ts(165,22): error TS2345: Argument of type 'string | undefined' is not assignable to parameter of type 'string'.`
because `t.writes[0]` is `string | undefined` under
`noUncheckedIndexedAccess`. Make the file type-check without weakening
the test.

## Context
- Line ~165: `expect(stripAnsi(t.writes[0])).toBe('abcdefghijklRRRRRR');`.
  Use a checked access (`const first = t.writes[0]; expect(first).toBeDefined();
  expect(stripAnsi(first ?? '')).toBe(...)`) or a non-null assertion — either
  is fine. Grep the file for any other `writes[0]` passed to `stripAnsi`.

## Acceptance criteria
- [ ] `npx tsc --noEmit` exits 0 and `bash scripts/check.sh` passes the
      `typecheck` and `unit` stages.
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/screen.test.ts` exits 0
      (the test still asserts the same string).
- [ ] No files changed outside scope

## Out of scope
Everything else.

## Worker report

> claimed_by: ds (pid 1999754) at 2026-09-03T17:28:53Z

### Summary

Fixed the `TS2345` type error in `tests/screen.test.ts` that was breaking
the `typecheck` stage of `scripts/check.sh`.

### Files changed

- `tests/screen.test.ts` — at the repaint test (the only `writes[0]` passed
to `stripAnsi`), replaced the unguarded `stripAnsi(t.writes[0])` with a
checked access: `const first = t.writes[0]; expect(first).toBeDefined();
expect(stripAnsi(first ?? '')).toBe('abcdefghijklRRRRRR');`. Assertion string
unchanged; test behaviour preserved.

### Commands run

- `bash .tigerteam/scripts/run-tests.sh tests/screen.test.ts` →
  `Test Files  1 passed (1)` / `Tests  12 passed (12)` (exit 0).
- `npx tsc --noEmit` → exit 0.
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok /
nethack-lib: ok / bridge: ok / bridge-smoke: ok` (all stages pass).

### Acceptance criteria

- [x] `npx tsc --noEmit` exits 0 and `bash scripts/check.sh` passes the
      `typecheck` and `unit` stages.
- [x] `bash .tigerteam/scripts/run-tests.sh tests/screen.test.ts` exits 0
      (same assertion string `abcdefghijklRRRRRR`).
- [x] No files changed outside scope (only `tests/screen.test.ts`).

### Doc changes

None (ticket names no doc file).

### Commit

- `2c643e1` on branch `tigerteam/T-0013`

### Reviewer notes

No out-of-scope issues noticed. Only `tests/screen.test.ts` changed.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds | deepseek-ai/DeepSeek-V4-Flash-0731 | 1m 23s | 107.26k (94.21k) | 2.02k | $0.0032 | engine | review |

total: 1 attempt · 1m 23s · 107.26k in (94.21k cached) / 2.02k out · $0.0032 (accepted 2026-09-03T17:30:36Z)
