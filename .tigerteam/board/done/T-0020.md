---
id: T-0020
title: Repaint immediately on terminal resize
priority: P1
complexity: C1
depends_on: []
scope: [src/ui/app.ts, src/term/screen.ts, src/term/tty.ts, tests/ui-resize.test.ts]
---

## Goal
Resizing the terminal leaves stale fragments until the next keypress,
game event or `Ctrl+L`. `Screen`'s constructor registers the only
`TermIO.onResize` callback and just calls `invalidate()`; the `App` never
learns about the resize, so nothing recomposes or paints. Make a resize
recompose and paint at once (PM reproduced it in tmux: shrink 215×68 →
120×40 left the old map fragments and no status rows; `Ctrl+L` fixed it).

## Context
- `src/term/tty.ts` (`onResize` stores a single callback), `src/term/screen.ts`
  (`Screen` constructor: `term.onResize(() => this.invalidate())`),
  `src/ui/app.ts` (compose + `screen.paint`; `Ctrl+L` path is the model:
  invalidate then repaint).
- Fix: let `TermIO.onResize` accept multiple listeners (keep the `off`
  bookkeeping in `tty.ts` `restore()`), and have `App` subscribe: on
  resize → `screen.invalidate()` → recompose for the new `columns/rows`
  → `paint` immediately (the too-small banner path when < 80×24 also
  goes through here). Debounce nothing; a resize storm is just a few
  full repaints.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/ui-resize.test.ts` exits 0
- [ ] One test, `resize repaints without any other event`: fake `TermIO`
      at 100×30 with a fixture-driven session painted once; change the
      fake's size to 120×40 and fire its resize listeners; assert a new
      full paint happened (`CSI 2 J` in the writes) and the status rows
      are on rows 39–40 — with no key and no session event in between.
- [ ] Manual check (report): in tmux, `tmux resize-window` while the game
      runs repaints the map and status rows immediately.
- [ ] No files changed outside scope

## Out of scope
Anything else in the App or modes.

## Worker report

> claimed_by: opus-1 (pid 2044552) at 2026-09-03T20:24:18Z

### Files changed

- `src/term/tty.ts` — `TtyTerm.onResize` now pushes into a `resizeCbs` array
  and the stdout `resize` listener iterates every registered callback, so
  both `Screen` (invalidate) and `App` (recompose + paint) can subscribe.
  `restore()` still drops the stdout listener (unchanged).
- `src/term/screen.ts` — updated the `TermIO.onResize` doc-comment to note
  that multiple listeners are supported and to record who each subscriber
  is (Screen invalidates the buffer; App recomposes and paints).
- `src/ui/app.ts` — `App` constructor subscribes to `term.onResize`:
  `screen.invalidate()` + `repaint()`. The recompose reads `term.columns`
  and `term.rows` fresh, so the too-small banner path is also exercised
  when the terminal shrinks below 80×24.
- `tests/ui-resize.test.ts` — new test: `App — resize repaints immediately`
  → `resize repaints without any other event`.

### Commands run

- `bash .tigerteam/scripts/run-tests.sh tests/ui-resize.test.ts`
  → `Test Files 1 passed (1)` / `Tests 1 passed (1)` (exit 0).
- `bash .tigerteam/scripts/run-tests.sh` (full suite regression)
  → `Test Files 17 passed | 1 skipped (18)` / `Tests 164 passed | 3 skipped
  (167)` — the 3 skips are `tests/bridge.test.ts` (needs a built bridge).
- `npx tsc --noEmit` → exit 0.

### Doc changes

- The `TermIO.onResize` JSDoc in `src/term/screen.ts` now documents the
  multi-listener contract (Screen invalidates; App recomposes and paints).
  No `docs/*.md` file was named by the ticket for update.

### Commit

- Branch `tigerteam/T-0020`, commit `a209d95` — 4 files changed, 132 (+)
  / 3 (-). Scope-only add: `src/term/screen.ts`, `src/term/tty.ts`,
  `src/ui/app.ts`, `tests/ui-resize.test.ts`.

### Acceptance walkthrough

1. `bash .tigerteam/scripts/run-tests.sh tests/ui-resize.test.ts` exits 0.
2. `resize repaints without any other event` (tests/ui-resize.test.ts):
   fixture-driven session, paint at 100×30, `term.resize(120, 40)` fires
   the resize listeners; asserts `after.width/height === 120/40`, that
   the writes after the resize contain `\x1b[2J`, and that rows 38–39
   (1-indexed 39–40) hold the composed status lines.
3. Manual tmux check: I cannot drive tmux from inside the worker, so this
   is flagged for the PM. The code path is exactly the one the ticket
   describes — `SIGWINCH` → `process.stdout` `resize` → `TtyTerm.resize`
   listener → App's resize callback → `screen.invalidate()` + `repaint()`
   (which reads `term.columns/rows` freshly). The unit test exercises
   the same App + resize chain against a `FakeTerm`.
4. Files changed are all inside the ticket's `scope`.

### Reviewer notes

- Multi-listener bookkeeping is intentionally minimal: no `off`/unsubscribe
  API. `restore()` still detaches the stdout `resize` handler in `tty.ts`,
  so the array becomes unreachable once the process is torn down. If the
  App ever needs to be recreated inside a single process, we'd want to add
  `offResize`, but that's out of scope for this ticket.
- Screen's own resize listener still calls `invalidate()`, so `App`'s
  `screen.invalidate()` is redundant but idempotent — kept in App to keep
  the "resize → full paint" invariant local to the App wiring rather than
  implicit on Screen registering first.
- Resize storm behaviour: no debouncing (per the ticket). Each SIGWINCH
  drives one full recompose + paint, which is a few hundred µs at 120×40.

> handoff: review

## Cancelled (PM, 2026-09-03)
User decision: repainting on the next event is good enough; not worth the extra plumbing. Branch `tigerteam/T-0020` not merged; worktree removed.

## Reinstated (PM, 2026-09-04)
User: "merge 0020 - it's already done so why not". Branch restored from a209d95 and merged.

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-4-7 | 5m 47s | 3.69M (3.69M) | 20.94k | $3.40 | engine | review |

total: 1 attempt · 5m 47s · 3.69M in (3.69M cached) / 20.94k out · $3.40 (accepted 2026-09-03T21:03:55Z)
