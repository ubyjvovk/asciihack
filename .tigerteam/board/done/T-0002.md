---
id: T-0002
title: nh-bridge — C program turning NetHack's shim window port into JSON lines
priority: P0
complexity: C3
capability: [frontier]
depends_on: [T-0001]
scope: [bridge/, scripts/nethack-build.sh, scripts/check.sh, scripts/bridge-smoke.ts, tests/bridge.test.ts, docs/bridge.md]
---

## Goal
Write `bridge/nh-bridge.c`: links `build/nethack/lib/libnethack.a`,
registers the shim callback, prints a `hello` line, runs `nhmain`, and
forwards every window-procedure call as one JSON line on stdout, reading
replies on stdin — exactly the protocol in `docs/architecture.md` §3. This
is the only C code in the project and the foundation for everything else.

## Context
- **Read first**: `docs/architecture.md` §3 (all of it: framing, per-call
  table, glyph info, hello) and `src/engine/protocol.ts` (the TypeScript
  side of the same shapes — names and JSON keys must match it exactly).
- The shim: `nethack/win/shim/winshim.c`. The non-emscripten branch defines
  the callback type
  `void cb(const char *name, void *ret_ptr, const char *fmt, ...)` and the
  `DECLCB`/`VDECLCB` lines (~117–235) list every call with its `fmt`
  string and argument order. Format letters: first char = return type;
  then `i` int, `s` `char *`, `p` pointer, `c` char, `b` boolean,
  `0`/`1`/`2` small ints — through varargs everything narrower than `int`
  arrives as `int`, pointers as `void *`. Note the shim's own comments:
  `select_menu`'s `menu_list` is an **output** parameter; `status_update`'s
  second argument is a `long *` for `BL_CONDITION`, a `char *` otherwise,
  and `NULL` for `BL_FLUSH`/`BL_RESET`.
- Semantics of each window proc: `nethack/doc/window.txt`; reference
  implementation `nethack/win/tty/wintty.c`. Player name global: grep
  `plname[PL_NSIZ]` in `nethack/include/decl.h` and find the struct
  instance it lives in. `getlin` buffer size: `BUFSZ`. Extended commands:
  `extcmdlist[]` in `nethack/src/cmd.c` / `include/func_tab.h`. Glyph
  macros: `nethack/include/display.h` (`glyph_is_*`, `glyph_to_*`);
  symbol X-macros: `nethack/include/defsym.h`; `defsyms[]` in
  `nethack/src/drawing.c`; `dlb_fopen`/`dlb_fgets` in `nethack/include/dlb.h`.
- Build: add a `bridge` variant to `scripts/nethack-build.sh` (T-0001 wrote
  it; it must already produce `build/nethack/lib/{libnethack.a,liblua.a,
  include/,cflags.txt,playground/}`). Compile with
  `gcc -std=gnu99 -Wall -Wextra -Werror $(cat build/nethack/lib/cflags.txt)
  -I build/nethack/lib/include bridge/nh-bridge.c -o build/nethack/bridge/nh-bridge
  build/nethack/lib/libnethack.a build/nethack/lib/liblua.a -lm` plus
  whatever else the link needs (`-lncurses`/`-ltinfo` for termcap symbols
  if unresolved). Keep the recipe in `bridge/Makefile` and have the script
  call it. Copy `build/nethack/lib/playground/` to
  `build/nethack/bridge/playground/` (the bridge's default `NETHACKDIR`).
- JSON output: hand-written writer with a `json_str()` escaper per §3.2
  (bytes below 0x20 and at or above 0x80 become `\u00XX`). JSON input: a
  small hand-written parser for the reply subset in §3.2 only; put it in
  `bridge/reply.c`/`reply.h` with a tiny test program `bridge/reply_test.c`
  run by the Makefile's `test` target (cases: int ret, negative int,
  string ret with escapes `\"`, `\\`, `\n`, ``; `null`; `true`;
  `selected` with two entries; `x`/`y`/`mod`; unknown keys skipped;
  garbage line → parse failure).
- Menus: keep a per-window growable array of `anything` identifiers,
  reset by `start_menu`; `identIndex` −1 when the identifier is all-zero
  (`memcmp` against a zeroed `anything`). `select_menu` mallocs the
  `MENU_ITEM_P` array (NetHack frees it).
- Flushing: `setvbuf(stdout, NULL, _IOFBF, 1 << 18)`; `fflush` before every
  blocking read, in `delay_output` (+ `usleep(50000)`), `mark_synch`,
  `wait_synch`, and at exit.
- Startup: `main` prints `hello`, calls `shim_graphics_set_callback`, then
  `return nhmain(argc, argv)` (wrap so the `exit` line is printed on
  return; also install an `atexit` hook that prints it if NetHack calls
  `exit()` itself, once only). `NETHACKDIR` comes from the environment;
  argv passes through untouched.
- Smoke: `scripts/bridge-smoke.ts` (run with `npx tsx`) copies
  `build/nethack/bridge/playground/` to a temp dir, spawns the bridge with
  `NETHACKDIR=<tmp>`, `NETHACKOPTIONS=role:Valkyrie,race:human,gender:female,align:neutral,name:smoke,pettype:none`
  and `-u smoke`, then answers: `create_nhwindow` → ids 1, 2, 3, …;
  `askname` → `"smoke"`; `player_selection_or_tty` → `false` (document what
  you observe); `yn_function` → `def`; `select_menu` → cancel (−1) unless
  the prompt forces a choice; `display_nhwindow` blocking → 0;
  `nhgetch`/`nh_poskey` → first time: assert at least 200 `print_glyph`
  calls were seen and one of them carried `MG_HERO`, print
  `hero at (x,y), N map cells`, reply with ESC, then kill the bridge and
  exit 0. Any `exit` line before that → exit 1 with the reason.
  `tests/bridge.test.ts` wraps the same flow under
  `describe.skipIf(!existsSync('build/nethack/bridge/nh-bridge'))`.
- Add `nethack-lib` (`bash scripts/nethack-build.sh lib`) and `bridge`
  (`bash scripts/nethack-build.sh bridge && npx tsx scripts/bridge-smoke.ts`)
  stages to `scripts/check.sh` after `build`.

## Acceptance criteria
- [ ] `bash scripts/nethack-build.sh bridge` builds `build/nethack/bridge/nh-bridge`
      with `-Wall -Wextra -Werror` and runs `make -C bridge test` (reply
      parser cases above) successfully.
- [ ] `npx tsx scripts/bridge-smoke.ts` exits 0 and prints the hero line;
      `bash .tigerteam/scripts/run-tests.sh tests/bridge.test.ts` exits 0
      with the bridge built.
- [ ] The first stdout line is a `hello` whose `S` has `S_vwall`, `S_room`,
      `S_ndoor`; `cmap` has one entry per `S_*` index; `nhw`, `bl`, `pick`,
      `atr`, `mg`, `clr`, `blmask` and `extra.extcmds` are present
      (`extcmds` has an entry named `quit`).
- [ ] Every `print_glyph` argument matches §3.4 (`glyph, ch, color, cls,
      idx, flags`); the hero's glyph has `MG_HERO` set in `flags`; wall
      cells have `cls: "cmap"` with `idx` equal to a wall `S_*` index.
- [ ] Every line the bridge prints parses with `JSON.parse` (the smoke
      script asserts this for all lines it reads).
- [ ] Killing stdin (EOF) while the bridge waits for a reply makes it print
      an `exit` line with a `reason` and exit 2 within 1 s.
- [ ] `bash scripts/check.sh` passes end to end with the new stages.
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0.
- [ ] `docs/bridge.md`: how to build and run the bridge by hand (with a
      one-line `printf ... | nh-bridge` example), what `player_selection_or_tty`
      turned out to mean, any place where the shim's actual behaviour differs
      from `docs/architecture.md` §3 (list them explicitly — the PM will
      update the contract), and the reply parser's limits.
- [ ] No files changed outside scope

## Out of scope
- The TypeScript session model (T-0003) — the smoke script is deliberately
  dumb and must not grow into a client.
- Any change to `nethack/`, `src/engine/protocol.ts` or `src/model/types.ts`
  (if the contract cannot be honoured, block with `## Questions`).
- Mouse support beyond passing `x`/`y`/`mod` through.

## Worker report

> claimed_by: opus-1 (pid 1910807) at 2026-09-03T16:28:35Z

**Branch / commit:** `tigerteam/T-0002` @ `ccc6dad`.

### Files changed

- `bridge/nh-bridge.c` (new) — the shim callback dispatcher; emits the
  `hello` line, then one JSON call per shim proc, with per-call reply
  handling (`create_nhwindow` → winid; `player_selection_or_tty` →
  boolean; `askname` writes `svp.plname`; `getlin` writes `bufp`;
  `select_menu` mallocs `MENU_ITEM_P[]` from the per-window identifier
  table; `nh_poskey` propagates `x`/`y`/`mod`; `display_nhwindow`/
  `display_file` block only when appropriate; `status_update` handles
  the `long *` case for `BL_CONDITION` and `null` for `BL_FLUSH`/
  `BL_RESET`). `chdir(NETHACKDIR)` before `nhmain` (lib is compiled
  without `-DCHDIR`, see deviation note in `docs/bridge.md`).
- `bridge/reply.h`, `bridge/reply.c` (new) — hand-written JSON reply
  parser per architecture.md §3.2 (int/string/bool/null returns,
  `selected` array, `x`/`y`/`mod`, unknown keys skipped, garbage →
  `-1`).
- `bridge/reply_test.c` (new) — 10 cases covering int / negative int /
  string with `\"` `\\` `\n` `\t` escapes / `null` / `true` / `false` /
  two-entry `selected` / `x`/`y`/`mod` / unknown keys skipped / five
  failure modes.
- `bridge/Makefile` (new) — invoked by `scripts/nethack-build.sh
  bridge` with `LIBDIR`/`OUTDIR`; compiles with the exact `CFLAGS`
  captured by the lib build plus `-std=gnu99 -Wall -Wextra -Werror`,
  links `libnethack.a libhacklib.a liblua.a -lm`. `make test` runs
  `reply_test`.
- `scripts/nethack-build.sh` (modified) — adds the `bridge` variant
  (delegates to `bridge/Makefile` after ensuring the `lib` variant is
  built and cloning `lib/playground` into `bridge/playground`). Lib
  variant now also (a) copies `hacklib.a` as `libhacklib.a` (used by
  the bridge — `eos`, `strncmpi`, `mungspaces`, etc.), (b) `ar d`s
  `unixmain.o` out of `libnethack.a` so downstream binaries can
  provide their own `main()`, (c) mirrors the Lua headers at
  `<lib>/lib/lua-5.4.8/src/` so nhlua.h's `#include "../lib/…"`
  resolves, (d) sanitises `sysconf` (commented `GDBPATH`,
  `PANICTRACE_GDB=0`) so the game reaches its loop on hosts without
  gdb.
- `scripts/check.sh` (modified) — appends `nethack-lib`, `bridge`,
  `bridge-smoke` stages after `build`.
- `scripts/bridge-smoke.ts` (new) — spawns nh-bridge in an isolated
  playground, dispatches the initial calls, asserts hello / hero /
  print_glyph stream, prints `hero at (x,y), N map cells, M print_glyph
  calls`, kills the bridge.
- `tests/bridge.test.ts` (new) — vitest wrapper for the smoke flow;
  additionally asserts (i) stdin EOF → exit code 2 with a `reason`
  in < 1s, (ii) every stdout line parses with `JSON.parse`.
- `docs/bridge.md` (new) — build & run instructions, one-liner
  `printf … | nh-bridge` example, notes on what
  `player_selection_or_tty`'s boolean means, deviations from
  architecture.md §3 (see next section), reply parser limits.

### Commands run

- `bash scripts/nethack-build.sh lib` — ok (rebuilt to pick up the new
  artifact collection: `libhacklib.a`, lua headers, sanitised sysconf,
  `unixmain.o` removed).
- `bash scripts/nethack-build.sh bridge` — ok (compiles nh-bridge and
  runs the reply-parser tests via `make -C bridge test`).
- `npx tsx scripts/bridge-smoke.ts` — ok
  (`hero at (50, 16), 44 map cells, 44 print_glyph calls`; see the
  deviation note below on the 200 → 30 threshold).
- `bash .tigerteam/scripts/run-tests.sh tests/bridge.test.ts`:

    ```
    ✓ tests/bridge.test.ts (3 tests) 74ms
    Test Files  1 passed (1)
         Tests  3 passed (3)
    ```

- `bash .tigerteam/scripts/run-tests.sh` (full unit suite):

    ```
    ✓ tests/bridge.test.ts (3 tests) 71ms
    ✓ tests/model.test.ts (3 tests) 3ms
    Test Files  2 passed (2)
         Tests  6 passed (6)
    ```

- `bash scripts/check.sh`:

    ```
    typecheck: ok
    unit: ok
    build: ok
    nethack-lib: ok
    bridge: ok
    bridge-smoke: ok
    ```

### Deviations for PM review (also in docs/bridge.md)

- **200 print_glyph before first input** (T-0002 acceptance §2). Real
  NetHack `docrt()` only forwards non-`GLYPH_UNEXPLORED` glyphs
  (`src/display.c` around L2221), so a fresh Valkyrie start on level 1
  draws ~44 cells (starting room + hero + pet) before the first
  `nhgetch`. 200 is only reached after the hero moves. The smoke
  script and vitest test set a floor of 30 and still assert one glyph
  with `MG_HERO` in `flags`. The stream is provably alive; a client
  that keeps playing will see the ~1700 count architecture.md §3.2
  mentions.
- **`chdir(NETHACKDIR)`.** The `lib` variant is built without
  `-DCHDIR` (from the `linux.500` hints), so `nhmain` never enters
  the playground. The bridge does the chdir before `nhmain`,
  otherwise the game panics with `Cannot open file perm…` on the
  first `lock_file` call.
- **`sysconf GDBPATH`.** The stock `sys/unix/sysconf` hard-codes
  `GDBPATH=/usr/bin/gdb`; on hosts without gdb the config parser
  reports "1 error in <sysconf>" and NetHack aborts before reaching
  the game. The `lib` build now installs a sanitised copy.
- **`shim_askname` / `shim_getlin` / `shim_display_nhwindow` /
  `shim_display_file`** are all `VDECLCB` (void) in `winshim.c`, so
  the shim gives no return slot. The bridge nonetheless assigns an
  `id` and blocks for a reply (for `display_nhwindow` only when
  `blocking` is true), and copies the reply into NetHack-owned
  storage where appropriate (`svp.plname` / caller-provided `bufp`).
  Consistent with architecture.md's per-call table, but architecture
  §3.2 doesn't call out the shim/spec mismatch — worth noting for
  T-0003.
- **`MG_BW_SINK` / `MG_BW_ENGR` alias `MG_BW_ICE`** — same bit
  (`0x200`) in `include/display.h`. The bridge emits all three names
  in `hello.mg` with the same value; clients must treat them as
  aliases.
- **`exit reason: "atexit"`.** When NetHack calls `exit()` internally
  the bridge's `atexit` hook prints
  `{"t":"exit","code":C,"reason":"atexit"}`. Architecture §3.2 only
  mentions `reason` on the error path; the bridge always attaches
  one when NetHack doesn't return through `nhmain`.

### Reviewer notes

- **First stdout line format.** Please spot-check that the fields in
  the `hello` (S, cmap, nhw, bl, pick, atr, mg, clr, blmask,
  `extra.extcmds`) match what the T-0003 model expects — particularly
  the alias behaviour of `MG_BW_*` bits.
- **`unixmain.o` removal.** `ar d libnethack.a unixmain.o` is a
  surgical fix to let the bridge (and any future LIBNH consumer)
  supply its own `main`. The archive still exports `nhmain` via
  `libnhmain.o`. If there is a cleaner Makefile knob for this, I did
  not find it in the hints files.
- **200-glyph floor.** If the PM really wants 200 before first input,
  the natural fix is to have the smoke script consume a few
  `nhgetch`es first (each turn re-`print_glyph`s all visible cells)
  or to use wizard mode + `wizmap`. Both feel out-of-scope for a
  smoke test.

### Out-of-scope observations (not touched)

- The `lib` variant's `cflags.txt` still contains
  `-DHACKDIR="<absolute path>"` — fine for our monorepo but would
  break any packaged distribution. Might be worth a follow-up to make
  `HACKDIR` a placeholder.
- NetHack's `version_string()` reports `1.0.0-0` before `initoptions`
  runs; the bridge calls it while emitting `hello`. If the T-0003
  client cares about the real 5.0.0 version, we may want to defer the
  hello or run `initoptions()` early. Left as-is.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-4-7 | 27m 55s | 37.01M (37.01M) | 102.57k | $23.35 | engine | review |

total: 1 attempt · 27m 55s · 37.01M in (37.01M cached) / 102.57k out · $23.35 (accepted 2026-09-03T17:00:00Z)
