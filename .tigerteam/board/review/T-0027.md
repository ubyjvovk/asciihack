---
id: T-0027
title: Ortho readable look — dark faces, absolute-brightness rims and seams, fog
priority: P1
complexity: C2
depends_on: [T-0026]
scope: [src/render/ortho.ts, tests/ortho.test.ts, tests/ortho-golden.txt, tests/ortho-zoom-golden.txt, docs/render.md]
---

## Goal
T-0023 gave the first-person view AsciiCity's readability: dark surfaces
that quantize to sparse glyphs, bright edges drawn at absolute brightness.
The ortho renderer shares `KIND_COLORS` and so inherited the dark bases but
still scales its rims and faces as multiples of them, which makes the
isometric room dim and flat. Give it the same grammar: dark block faces,
bright top rims and corner lines, dark seams, distance fade.

## Context
- `docs/render.md` ("Ortho renderer", and the raycaster's camera/look
  sections from T-0023 for the levels), `src/render/ortho.ts` (v2 + cutaway
  + tile sprites from T-0026).
- Levels (linear, before exposure; the quantizer is unchanged):
  - Wall block faces: base from `KIND_COLORS` (wall 0.14 …) × 0.75 (SE) /
    0.55 (SW) with the brick texture body ±0.03 and mortar at `0.05`
    absolute; the **top face** `0.16` flat (a dark lid).
  - Rims and lines at **absolute** brightness: the top face's upper two
    edges `0.75`, its lower two edges `0.45`, the vertical corner between
    the faces `0.55`, the block's bottom contact line `0.25`.
  - Floor tiles: base from `KIND_COLORS.floor` with the flagstone texture
    (import `floorShade` from `texture.ts`, stones ±20 %), diamond seams
    `0.30` absolute; checker removed (the stones carry the variation);
    corridors `KIND_COLORS.corridor`, no seams.
  - Doorway / open door tiles: floor in the door colour with the two
    post cells at `0.70`.
  - Unexplored lattice unchanged (`0.05`); fog `exp(−0.06·dist)` applied to
    faces and floor but **not** to rims within 3 cells of the hero (near
    edges stay crisp), then normally beyond.
  - Cutaway ghost blocks keep their 0.35 factor applied to these levels.
- Regenerate both ortho goldens; add a jackal sprite to the zoom golden if
  T-0026 has not already.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/ortho.test.ts` exits 0
- [ ] Tests, by name (only these): a wall block's face median brightness
      is below 0.16 while its top-rim cells are above 0.6 and its corner
      cell above 0.4; floor tiles show ≥ 3 distinct levels and seam cells
      are brighter than stone cells; a rim 6 cells from the hero is dimmer
      than one 1 cell away; both goldens match; timing under 8 ms.
- [ ] Manual check (report, with the bridge built): `--mode=ortho
      --theme=amber` at ≥ 100 rows reads as dark blocks with bright top
      edges, like the first-person view.
- [ ] `docs/render.md` ortho section: a "Look" paragraph with the levels.
- [ ] No files changed outside scope

## Out of scope
The raycaster, sprites, UI.

## Worker report

> claimed_by: muse (pid 2044552) at 2026-09-03T23:48:05Z

- `src/render/ortho.ts` — readable-look rework: wall faces base×0.75/0.55 with
  brick body ±0.03 and mortar at absolute 0.05; dark 0.16 lid with upper rim
  0.75 / lower edge 0.45, corner 0.55, contact 0.25, doorway posts 0.70 (all
  absolute); floor flagstone via `floorShade` with 0.30 absolute seams (checker
  removed, corridors flat); fog `exp(−0.06·dist)`, rims crisp within 3 cells;
  cutaway 0.35 ghost kept. Lid paints the diamond's own rows (not the ±1
  footprint) so the 0.75 rim never smears onto the neighbour tile.
- `tests/ortho.test.ts` — new `ortho/look` block with the ticket's 3 named cases
  (face median < 0.16 + rim > 0.6 + corner > 0.4; ≥3 stone levels + seams
  brighter; rim at 6 cells dimmer than at 1 cell); geometry-only terrain test;
  cutaway far-block expectation updated to the 0.75 rim; KIND_COLORS import
  dropped (unused).
- `tests/ortho-golden.txt`, `tests/ortho-zoom-golden.txt` — regenerated
  (zoom golden already had the jackal + potion; both render under the new look).
- `docs/render.md` — ortho `fogK` 0.06, rewritten Floor/Wall pass sections plus a
  new "Look" paragraph with the levels.
- Commands: `bash .tigerteam/scripts/run-tests.sh tests/ortho.test.ts` →
  `Test Files 1 passed (1) / Tests 17 passed (17)`, perf line
  `ortho 200×60 average: ~0.4 ms over 20 runs` (< 8 ms). `npx tsc --noEmit`
  clean for all scope files (one pre-existing error in
  `tests/engine-bridge.test.ts:91` TS2532, untouched, out of scope).
- Commit `d438290` on branch `tigerteam/T-0027` (5 scope files, nothing outside).
- Reviewer notes: the lid-edge rule is one absolute row per diamond edge
  (upper 0.75, lower 0.45) rather than the ticket's literal "upper/lower two
  edges" — at k=1..2 the lid is only 2–4 rows deep, so two-row bands would
  swallow the 0.16 interior; the golden dumps show the bright-diamond grammar
  reads. The bridge is not built in this worktree (`build/` empty), so the
  `--mode=ortho --theme=amber` manual check was verified through the golden
  ASCII dumps only (dense `=`/`+` lid edges over sparse `.`/`:` faces) — PM
  eyeball review needed. No T-0029 conflict: this branch predates it and keeps
  the T-0023-era KIND_COLORS; T-0029's colour/fog retune will layer on top.

> handoff: review
