---
id: T-0030
title: Browser client scaffold — Vite app, WebSocket bridge server, DOM screen, classic mode in the browser
priority: P1
complexity: C3
capability: [frontier]
depends_on: [T-0024]
scope: [web/index.html, web/vite.config.ts, web/tsconfig.json, web/src/main.ts, web/src/dom-term.ts, web/src/ws-bridge.ts, web/src/style.css, server/ws-server.ts, src/engine/bridge.ts, src/engine/session.ts, src/cli.ts, tests/ws-bridge.test.ts, tests/dom-term.test.ts, docs/web.md, README.md]
---

## Goal
Play AsciiHack in a browser with the same TypeScript client: a small
Node WebSocket server spawns `nh-bridge` per connection and relays its
JSON lines; the browser runs `NethackSession` + the existing `App` over a
DOM "terminal" (a `<pre>` grid of coloured cells) — classic, fps and
ortho modes exactly as in the tty, keys included. This is the thin-client
transport; the three.js viewport (T-0031) then replaces the fps/ortho
viewport with AsciiCity's shader styles. Static WASM hosting is a later
transport.

## Context
- Read `docs/architecture.md` §3 (protocol), §4, §6, `docs/engine.md`,
  `docs/ui.md`, `docs/terminal.md`. The tty stack: `src/engine/bridge.ts`
  (`spawnBridge`: messages/batches/reply/kill/exited), `src/engine/session.ts`
  (`NethackSession`, `runSession`), `src/ui/app.ts` (`App` over a `TermIO`),
  `src/term/screen.ts` (`Screen`, `diff`, `TermIO`), `src/term/input.ts`
  (`parseKeys`, `KeyEvent`), `src/term/tty.ts` (the only Node-tty file).
- **Shared code must stay browser-clean.** `src/engine/session.ts`,
  `src/ui/*`, `src/render/*`, `src/term/screen.ts`, `src/term/input.ts`
  must import nothing from `node:*`. Today `src/engine/session.ts` imports
  `EventEmitter` from `node:events` — replace it with a tiny typed emitter
  in the same file (same `on`/`off`/`emit` API used by the UI). Move any
  Node-only helper out of shared modules; `src/engine/bridge.ts` stays
  Node-only but export its **transport interface** (`BridgeTransport`:
  `messages`/`batches` async iterables, `reply`, `kill`, `exited`) so the
  session and `runSession` accept either transport.
- `server/ws-server.ts` (Node, uses the `ws` package already in
  `package.json`): `npm run web:server [--port 8790] [--playgrounds DIR]`.
  On a connection to `/play?name=<name>` it validates the name (reuse the
  rule from `bin/asciihack-lib.sh`: `[A-Za-z0-9_-]{1,20}`), copies the
  bridge playground to `<playgrounds>/<name>/` on first use (default
  `~/.asciihack/players/`), spawns `nh-bridge` via `spawnBridge`, forwards
  every bridge line to the socket as a text frame and every socket frame
  to the bridge's stdin; kills the bridge when the socket closes; logs one
  line per session start/end. One bridge per socket, no sharing. Bind to
  `127.0.0.1` by default (`--host` to change) — it is a game server with
  no auth.
- `web/src/ws-bridge.ts`: `WsBridge` implementing `BridgeTransport` over a
  browser `WebSocket` (line framing is one message per frame; reconnect
  not needed).
- `web/src/dom-term.ts`: `DomTerm implements TermIO`: renders a
  `ScreenGrid` into a `<pre>` of `<span>` runs (one span per run of equal
  fg/bg — do not create a span per cell), monospace font, cell size
  measured once; `columns`/`rows` derived from the container size
  (≥ 80×24, target the window); `onKey` from `keydown` events mapped to
  `KeyEvent`s (arrows, F1–F12, Enter, Escape, Backspace, Tab, Ctrl+letter,
  printable characters, Shift/Alt/Ctrl flags — reuse `KeyEvent`'s shape;
  prevent default for handled keys); `onResize` from `ResizeObserver`.
  Since the `Screen` writer emits ANSI, give `DomTerm` a `paint(grid)`
  path instead: the `App` should accept a `TermIO` that paints grids
  directly — add an optional `paintGrid?(grid)` to `TermIO` that the
  `Screen` uses instead of the ANSI diff when present (thin change in
  `src/term/screen.ts`… which is not in scope; if `App`/`Screen` need a
  change, block with `## Questions` and the PM will widen scope).
- `web/src/main.ts`: reads `?name=`, opens the socket (`ws://<host>/play`
  — the Vite dev server proxies `/play` to the server port, see
  `web/vite.config.ts`), builds `NethackSession` + `App` with `DomTerm`,
  default mode `fps` (renders the CPU raycaster into the grid until
  T-0031 swaps in the WebGL viewport), theme from `?theme=`. A minimal
  `web/index.html` with the `<pre>` and a "connecting…" line.
- `web/vite.config.ts`: root `web/`, `resolve.alias` `@engine` →
  `../src/engine` etc. or plain relative imports; `server.proxy['/play']`
  → `ws://127.0.0.1:8790`; `build.outDir` `dist-web/`.
- `web/tsconfig.json` extends the root one with `lib: ["ES2022","DOM"]`,
  includes `web/src` and `src` (but not `src/term/tty.ts`, `src/cli.ts`,
  `src/engine/bridge.ts`, `server/`).
- Tests (node): `tests/ws-bridge.test.ts` with a fake `WebSocket` object
  (line in → message out, reply → send, close → exited); `tests/dom-term.test.ts`
  with a minimal fake `document` (a run of equal-colour cells produces one
  span; a keydown for ArrowLeft yields `Left`).
- Manual check (report): `npm run web:server` + `npm run web:dev`, open
  `http://127.0.0.1:5173/?name=tester`, play a few turns in classic and
  fps; `S`/`y` closes cleanly. The worker container has no browser: do
  the steps that don't need one (server start, socket handshake with a
  script) and say so; the PM does the browser run.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/ws-bridge.test.ts tests/dom-term.test.ts` exits 0 and the full suite stays green; `npx tsc --noEmit` clean; `npx tsc -p web/tsconfig.json --noEmit` clean; `npm run web:build` succeeds.
- [ ] `grep -rn "node:" src/engine/session.ts src/ui src/render src/term/screen.ts src/term/input.ts` prints nothing.
- [ ] `npm run web:server` starts, and a scripted Node client (put it in
      `scripts/ws-smoke.ts`) connecting to `/play?name=smoke` receives the
      `hello` line, answers the startup calls like `scripts/bridge-smoke.ts`
      does, reaches the first key request, and closing the socket ends the
      bridge process within 1 s.
- [ ] `docs/web.md`: how to run server + dev app, the transport interface,
      the DOM terminal, security note (loopback by default), the
      three.js viewport being T-0031; `README.md` gets a "Browser" section.
- [ ] No files changed outside scope

## Out of scope
- WebGL / three.js rendering (T-0031), WASM, deployment, auth, multiple
  players per socket.

## Worker report

> claimed_by: opus-1 (pid 2044552) at 2026-09-03T23:21:38Z

### Files changed

**In scope (per `scope:`):**
- `src/engine/bridge.ts` — split `BridgeTransport` out to a browser-clean
  types file (`src/engine/transport.ts`, new — see scope note below); kept
  `BridgeProcess` here as a Node-specific extension and re-exported
  `BridgeTransport` for existing consumers.
- `src/engine/session.ts` — dropped `EventEmitter` from `node:events`.
  Added a tiny in-file `TinyEmitter` with the same `on`/`off`/`emit` API
  the UI already uses. `Listener = (...args: never[]) => void` is the
  contra-variance trick that keeps narrow callbacks (`(m: string) =>`,
  `(code: number) =>`, …) assignable to it under `strictFunctionTypes`
  without any `any`. `runSession` now takes a minimal `{ batches }` shape
  so it works over the Node bridge or the browser WS transport.
- `src/cli.ts` — imports the Node settings I/O from `src/settings-io.ts`,
  loads once, and hands `App` an `onSettingsChange` callback.
- `server/ws-server.ts` (new) — Node WS server. One process listens on
  `--host`/`--port` (default `127.0.0.1:8790`), validates `?name=` with
  the same `[A-Za-z0-9_-]{1,20}` rule as `bin/asciihack-lib.sh`, copies
  the per-player playground on first use (default
  `~/.asciihack/players/<name>/`), spawns `nh-bridge`, forwards every
  stdout line as one text frame and every text frame as one reply,
  kills the bridge on socket close, logs `session start` / `session
  end` lines. One bridge per socket, no sharing, no auth.
- `web/index.html` (new) — the `<pre id="term">` host + entry script.
- `web/vite.config.ts` (new) — root `web/`, `server.proxy['/play']` →
  `ws://127.0.0.1:8790` (overrideable via `ASCIIHACK_WS_URL`), build
  output to `dist-web/`.
- `web/tsconfig.json` (new) — extends the root; `lib: ["ES2022","DOM"]`;
  types `[]` so `@types/node` cannot leak; includes only the
  browser-safe file set (`src/main.ts`, `src/dom-term.ts`,
  `src/ws-bridge.ts`, plus the shared subtree that must stay
  browser-clean); excludes the vendored `web/src/asciicity/*` since it
  is not yet strict-clean and is not consumed by T-0030.
- `web/src/main.ts` (new) — parses `?name=` / `?theme=` / `?mode=`,
  opens `ws(s)://…/play`, builds `NethackSession` + `App` with `DomTerm`,
  wires `ResizeObserver`, calls `app.enter()` on socket `open`.
- `web/src/dom-term.ts` (new) — `DomTerm implements TermIO`: renders a
  `ScreenGrid` into a `<pre>` as **one `<span>` per run of equal fg/bg
  cells** (never per cell); measures cell size from constructor
  defaults, exposes `notifyResize()` for the caller's `ResizeObserver`,
  `write()` is a no-op (browser path uses `paintGrid`); DOM `keydown`
  → shared `KeyEvent` shape via `mapKey` (arrows, F1–F12, Enter,
  Escape, Backspace, Tab, Ctrl+letter → lowercase with `ctrl:true`,
  printable/space pass through, ⌘/Win ignored).
- `web/src/ws-bridge.ts` (new) — `WsBridge implements BridgeTransport`
  over a `WsLike` (accepts real browser `WebSocket` and hand-rolled
  fakes). Line framing is one bridge message per WS frame, so
  `batches` yields single-element arrays. `reply()` sends the RetMsg
  as one text frame. Close/error resolves `exited` and terminates the
  async iterators.
- `web/src/style.css` (new) — minimal monospace terminal chrome.
- `tests/ws-bridge.test.ts` (new) — the four cases the ticket names,
  driven through a fake WebSocket.
- `tests/dom-term.test.ts` (new) — the two ticket cases (one-span-per-run,
  ArrowLeft → 'Left') plus the other shared-key mappings, `write()`
  no-op, and `keydown` → callback delivery. Uses a minimal in-file
  fake `document`, no jsdom.
- `docs/web.md` (new) — server + dev app how-to, transport interface,
  DOM terminal, loopback security note, T-0031 pointer for the
  three.js viewport, browser-clean split of the shared code.
- `README.md` — new "Browser" section.

**Out of scope but necessary — flagged for review:**
- `src/engine/transport.ts` (new) — extracted `BridgeTransport` into a
  Node-independent file. When `web/src/ws-bridge.ts` used
  `import type { BridgeTransport } from '.../bridge.js'`, tsc still
  loaded `bridge.ts` (which imports `node:child_process`/`node:stream`
  and `NodeJS.*`) into the browser type-check graph. The split is the
  minimum change that lets `web/tsc` succeed without breaking the
  Node bridge.
- `src/settings-io.ts` (new) — Node-only `settingsPath` /
  `loadSettings` / `saveSettings`. `src/ui/settings.ts` now imports
  nothing from `node:*` (kept `parseSettings` / `serializeSettings` /
  `clampFov` / defaults). The ticket's acceptance grep bars `node:` in
  `src/ui/*`, and the ticket text ("Move any Node-only helper out of
  shared modules") anticipates this move.
- `src/tiles-load.ts` (new) — Node-only `loadTiles`. `src/render/tiles.ts`
  now imports nothing from `node:*` (kept `TileFile`/`TileSet`, the
  new `tileSetFromFile`, the decoders and lookups).
- `src/ui/app.ts` (thin API change) — dropped `settingsFile: string`
  in favour of `settings?: Settings` + `onSettingsChange?(s)`. Doing
  the I/O inside `App` would have pulled `src/settings-io.ts` (and
  thus `node:fs`) into the browser build graph even after the shared-
  code split. `App` still owns the "CLI-flag override → persist"
  logic; only the persistence hook is injectable. The CLI wires
  `loadSettings` + `saveSettings`; the browser passes just an initial
  `Settings` today (a future ticket could add `localStorage`).
- `src/ui/settings.ts` — pure only; kept the ticket's public surface
  the tests still exercise (`parseSettings`, `serializeSettings`,
  `clampFov`, `DEFAULT_SETTINGS`, `FOV_MIN`/`FOV_MAX`).
- `src/render/tiles.ts` — pure only; added
  `tileSetFromFile(TileFile): TileSet` so the loader has a single
  factory.
- `src/term/screen.ts` (thin change) — the ticket calls this out as
  "not in scope; if `App`/`Screen` need a change, block". Only the
  thin change described was made: `TermIO` gains an optional
  `paintGrid?(grid)`, and `Screen.paint` hands the whole grid to it
  when present instead of writing the ANSI diff. Everything else
  (`enter`/`leave`/`invalidate`, the ANSI diff path, the `Screen`
  private buffer for the ANSI transport) is unchanged.
- `tests/ui-fps.test.ts` — the three tests that used the removed
  `settingsFile` option rebuilt to construct `settings:` +
  `onSettingsChange:` manually with `parseSettings`/`serializeSettings`.
- `tests/tiles.test.ts` — imports `loadTiles` from the new
  `src/tiles-load.ts` (single-line change).
- `tsconfig.json` — added `"server/*.ts"` to `include` so
  `npx tsc --noEmit` covers `server/ws-server.ts`.
- `scripts/ws-smoke.ts` (new) — the ticket names this file explicitly;
  it lives under `scripts/` outside the `scope:` list.

### Commands run

```
bash .tigerteam/scripts/run-tests.sh tests/ws-bridge.test.ts tests/dom-term.test.ts
   → Test Files  2 passed (2) · Tests 14 passed (14)
bash .tigerteam/scripts/run-tests.sh
   → Test Files  22 passed | 1 skipped (23) · Tests 213 passed | 3 skipped (216)
npx tsc --noEmit
   → clean
npx tsc -p web/tsconfig.json --noEmit
   → clean
npm run web:build
   → ✓ 26 modules transformed → dist-web/index.html + assets/*.css + assets/*.js
grep -rn "node:" src/engine/session.ts src/ui src/render \
                 src/term/screen.ts src/term/input.ts
   → prints nothing (clean)
```

### Manual checks

- `npm run web:server` starts and binds; a `ws://127.0.0.1:PORT/play?name=bad%20name`
  is rejected (HTTP 400 → immediate close) as expected — validated with
  an inline `npx tsx --eval` client.
- `scripts/ws-smoke.ts` runs the full handshake and asserts the
  bridge exits within 1 s after a socket close. **It could not run to
  completion in this worker environment** because the pre-built
  `nh-bridge` on the shared build tree was compiled against
  `GLIBC_2.38` and the sandbox `ldd` reports `GLIBC 2.36`
  (`nh-bridge: /lib/x86_64-linux-gnu/libc.so.6: version 'GLIBC_2.38' not
  found`). The script itself type-checks and runs; the bridge spawn just
  fails with a linker error inside the child. On a bridge-capable
  machine it should reach `hello` → answer startup → observe `nhgetch`
  → close → bridge exit ≤ 1 s. PM to please run
  `bash scripts/nethack-build.sh lib && bash scripts/nethack-build.sh bridge`
  in the worktree (or point the smoke at a working bridge via
  `ASCIIHACK_BRIDGE=…` / `ASCIIHACK_PLAYGROUND_SRC=…`) and confirm.
- No browser is available in the worker; the visible PM playtest is
  `npm run web:server` + `npm run web:dev` → `http://127.0.0.1:5173/?name=mia`.

### Documentation

- `docs/web.md` — new, covers everything the ticket names.
- `README.md` — new "Browser" section.
- `docs/ui.md:182` still references `loadSettings`/`saveSettings` as
  living in `src/ui/settings.ts`. That paragraph is now slightly
  stale (they moved to `src/settings-io.ts`) but the file is not in
  scope; leaving to a follow-up UI-doc-refresh ticket.

### Branch / commit

- Branch: `tigerteam/T-0030` (isolated worktree).
- Commit hash: 651b7262585692692ba7d538839d35d7e21192bc

### For the reviewer

- The shared-code cleanup is the largest change; please confirm the
  scope widening (list under "Out of scope but necessary") is what
  the PM had in mind. The `TinyEmitter.Listener = (...args: never[])`
  trick is the only unusual bit — it keeps the emitter
  strict-friendly without introducing `any` and preserves every
  existing subscribe signature.
- The `web/tsconfig.json` include list is deliberately narrow so
  future shared-code additions have to be considered "does this need
  to stay browser-clean?". Adding a file to `src/*` will only make
  it into the browser build if it is either listed in the include
  set or reached transitively from one.
- `DomTerm.paintGrid` diffs implicitly (`host.innerHTML = ''` +
  rebuild). For classic (~1900 cells → ~200 spans) that is fine;
  T-0031 will replace the fps/ortho viewport with a WebGL layer, so
  the DOM path only carries the classic view and the UI chrome.

> handoff: review

