---
id: T-0018
title: Heading indicator — compass ribbon in fps, facing arrow on the minimap, rose in ortho
priority: P1
complexity: C2
depends_on: [T-0007]
scope: [src/ui/compass.ts, src/ui/minimap.ts, src/ui/modes/fps.ts, src/ui/modes/ortho.ts, tests/ui-compass.test.ts, tests/ui-fps.test.ts, docs/ui.md]
---

## Goal
In the first real session the user could not tell which way they were
facing. Add three cheap, always-visible cues: a compass ribbon along the
top of the first-person viewport (AsciiCity's BEARING idea), the hero drawn
as a facing arrow on the minimap, and a small fixed rose in the ortho view
(whose north is up-right on screen).

## Context
- `docs/ui.md` (mode interface, `paintViewport(grid, rect)`, minimap),
  `src/ui/modes/fps.ts` (owns `facing` and the interpolated `yaw`),
  `src/ui/minimap.ts`, `src/ui/modes/ortho.ts`, `src/ui/view3d.ts`
  (`FACINGS`), `src/ui/grid.ts` (`putText`). Coordinates: `docs/architecture.md`
  §7 (yaw 0 = N, +π/2 = E, clockwise).
- **Compass ribbon** (`src/ui/compass.ts`, pure: `paintCompass(grid, rect,
  yawRad)`): drawn on the first row of the viewport rect, centred, 41
  columns wide, over the rendered scene. Headings `N NE E SE S SW W NW`
  (angles 0°, 45°, …) are placed at column `centre + round((Δ / 35°) · 20)`
  where `Δ` is the signed angular difference heading − yaw normalised to
  (−180°, 180°]; only headings with |Δ| ≤ 35° are shown (that matches the
  70° field of view: what you see is what the ribbon spans); a `·` tick
  every 15° in between; the centre column shows `|` under the label. The
  nearest heading is drawn bright white, others dim grey (`[120,120,120]`),
  ticks darker; background black so it reads over any scene. It uses the
  interpolated yaw so it slides during a turn.
- **Minimap facing arrow** (`src/ui/minimap.ts`): `paintMinimap` takes an
  optional `facing` (the `Facing` from `FACINGS`); when given, the hero
  cell prints an arrow instead of `@`: `↑ ↗ → ↘ ↓ ↙ ← ↖` for N…NW
  (U+2191, U+2197, U+2192, U+2198, U+2193, U+2199, U+2190, U+2196; all
  BMP, one cell wide — fine for `ScreenCell.ch`), inverse-video as today.
  Without `facing` (ortho, classic) it stays `@`.
- **Ortho rose**: a fixed 5×3 legend at the top-left of the ortho
  viewport, dim grey with the `+` bright:
  ```
  W   N
    +
  S   E
  ```
  because in the locked projection north is up-right (cell (x, y−1) →
  screen (sx+2, sy−1)), east down-right, south down-left, west up-left.
- Keep it out of the classic mode. Nothing here touches the renderers.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/ui-compass.test.ts tests/ui-fps.test.ts` exits 0
- [ ] Tests, by name (only these): ribbon facing north shows `N` at the
      centre column and `NE`/`NW` at ±20 columns with `E`/`W` absent;
      facing south-west (yaw 225°) shows `SW` centred, `S` to the left and
      `W` to the right (ordering, not exact columns); the minimap prints
      `↓` on the hero cell when facing south and `@` without a facing; the
      fps mode after `Right` from north paints `NE` at the centre once the
      turn settles; the ortho viewport contains the rose rows.
- [ ] Manual check (report): in the live game the ribbon slides on
      `Left`/`Right` and the minimap arrow matches it.
- [ ] `docs/ui.md`: "Heading cues" subsection.
- [ ] No files changed outside scope

## Out of scope
- Renderer changes (T-0017), a heading in the NetHack status lines, mouse.

## Worker report

> claimed_by: ds-2 (pid 2044552) at 2026-09-03T20:03:03Z

### Files changed (all in scope)
- `src/ui/compass.ts` (new) — pure `paintCompass(grid, rect, yawRad)` ribbon: 41-col
  black strip on the viewport's first row, headings at `centre + round(Δ·20/45°)`,
  `·`/`|` ticks every 15°, nearest heading bright white / others `[120,120,120]` / ticks
  darker, uses the interpolated `yaw` so it slides during a turn.
- `src/ui/minimap.ts` — `paintMinimap` takes optional `facing`; the hero cell prints
  its facing arrow (`↑ ↗ → ↘ ↓ ↙ ← ↖`, all BMP one-cell) instead of `@` when given,
  stays `@` without.
- `src/ui/modes/fps.ts` — passes `this.facing` to the minimap and paints the compass
  (after the minimap, on top of the rendered scene).
- `src/ui/modes/ortho.ts` — paints the fixed 5×3 rose at the top-left of the viewport
  (`W N` / `+` / `S E`; north is up-right in the locked projection), dim grey with a
  bright `+`. Verified against `cellToScreen` (north = (sx+2, sy−1), east = (sx+2, sy+1),
  south = (sx−2, sy+1), west = (sx−2, sy−1)).
- `tests/ui-compass.test.ts` (new) — the two ribbon tests from the acceptance list.
- `tests/ui-fps.test.ts` — the three remaining acceptance tests (minimap arrow, fps
  turn→NE-at-centre, ortho rose rows); updated the two pre-existing fps structural tests
  (F4 toggle, ramp+minimap) to expect the facing arrow instead of `@`, since the fps
  minimap hero is now an arrow, not `@`.
- `docs/ui.md` — new "Heading cues" subsection + updated the mode-interface and Minimap
  sections.

### Commands / results
- `bash .tigerteam/scripts/run-tests.sh tests/ui-compass.test.ts tests/ui-fps.test.ts` →
  `Test Files 2 passed (2)`, `Tests 24 passed (24)`
- `bash .tigerteam/scripts/run-tests.sh` (full suite) → `Test Files 17 passed | 1 skipped (18)`,
  `Tests 168 passed | 3 skipped (171)`
- `npx tsc --noEmit` → exit 0; `npx tsc -p tsconfig.build.json` → exit 0
  (full `check.sh` not run — its nethack-lib/bridge stages need the submodule build,
  and this ticket touches no build/config or `src/cli.ts`)

### Docs
- `docs/ui.md`: "Heading cues" subsection (compass ribbon geometry + colours, minimap
  facing arrow, ortho rose), plus prose updates in the mode-interface and Minimap sections.

### Commit
- `127d7a9` on branch `tigerteam/T-0018` — `[T-0018] Heading indicator — compass ribbon
  in fps, facing arrow on the minimap, rose in ortho` (7 files, only scope paths).

### For the reviewer / things I could not verify
- **Spec/acceptance discrepancy (flagged, did not guess silently):** the Context's ribbon
  formula says `centre + round((Δ/35°)·20)` with "only headings with |Δ| ≤ 35° shown",
  which puts NE at ±26 and *excludes* NE/NW when facing north — directly contradicting
  the acceptance criterion "NE/NW at ±20 columns with E/W absent" and the SW ordering
  test. I implemented the acceptance criterion: scale 45°/20 columns (one facing step =
  20 cols), visibility cutoff at the 41-column span (±45°). If the PM wants the literal
  35°/70°-FOV mapping instead, that's a one-line change in `compass.ts` but fails the
  stated acceptance tests.
- "The centre column shows `|` under the label": I draw the `|` notch first and the
  heading labels over it, so when a heading is being faced exactly its label sits on the
  `|` (hidden beneath it); between headings the `|` shows as the facing notch. One row,
  as the Context specifies.
- **Manual live-game check not run:** the isolated worktree has no NetHack/bridge build
  (submodule empty) and my model cannot view rendered frames; I verified the ribbon/minimap
 /rose behaviour through the unit tests + structural grid checks. Please eyeball in the
  live game (`npm start -- --mode=fps`, Left/Right slides the ribbon, minimap arrow
  matches) as the acceptance criterion intends.
- Existing tests `F4 removes the minimap` and `fps viewport … minimap contains the hero
  facing arrow` were updated (not padded) because the fps minimap hero is now an arrow,
  so their old `@`-based assertions no longer applied.

> handoff: review
