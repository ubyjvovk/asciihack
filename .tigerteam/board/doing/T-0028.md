---
id: T-0028
title: Quantizer tone curve — black point and steeper gamma for every theme, desaturate dark cells
priority: P0
complexity: C2
depends_on: []
scope: [src/render/ascii.ts, src/render/themes.ts, tests/ascii.test.ts, tests/themes.test.ts, docs/terminal.md]
---

## Goal
The user's screenshot (142×75, cyber theme, hero in a corridor facing the
unknown): the never-seen space renders as a solid blue field of `?`, the
wall bodies as dense `(`/`)` fields, and the floor comes out yellow. The
renderer's levels are right (T-0023); the quantizer's tone curve is not:
`dens = v^0.45` with no black point turns 3 % brightness into a mid-density
glyph, and normalising the hue at full brightness turns any faint colour
cast in a dark cell into a saturated one. Give the quantizer a black
point, a steeper gamma and darkness-dependent desaturation, for every
theme, so dark surfaces quantize to sparse dots or nothing and stay grey.

## Context
- `docs/terminal.md` (quantizer + themes), `src/render/ascii.ts`
  (`glyphIndex`, `cellFromRgb`, `quantize`, `quantizeInto`,
  `QuantizeOptions`), `src/render/themes.ts` (`themeMix`, `amberMix`,
  `amberDensity`). Renderers and their goldens use test-local ramps and
  are not affected; UI tests that replay fixtures through the quantizer
  must stay green (adjust expectations only where a row legitimately
  becomes sparser).
- New `QuantizeOptions.blackPoint` (default `0.08`) and new defaults
  `gamma 0.7` (was 0.45), `exposure 1.7` unchanged. Density for cyber /
  gloom / solarized: `v = max(c)` after exposure, `vc = clamp((v −
  blackPoint) / (1 − blackPoint), 0, 1)`, `dens = vc^gamma`,
  `idx = round(dens · (ramp.length − 1))` — `vc = 0` is the space glyph.
  Amber keeps its own `amberDensity` (it already has a black point) but
  uses the same `blackPoint` option value in place of its hard-coded 0.06.
- Desaturation (all four themes, before the theme mix): `raw = c /
  max(v, 0.02)`; `grey = 0.299·raw.r + 0.587·raw.g + 0.114·raw.b`;
  `sat = clamp(dens · 1.5, 0, 1)`; `tint = grey + (raw − grey)·sat`, then
  the existing brightness fold `tint · clamp(dens·0.7 + 0.4)`. Dark cells
  come out grey, bright cells keep their hue. Overlay glyphs (sprites) are
  untouched — their colour is meaningful.
- Worked numbers (T-0023's levels, cyber): veil 0.03·1.7 = 0.05 → below
  the black point → space; wall body 0.14 → v 0.24 → vc 0.17 → dens 0.29
  → a sparse glyph (`,`/`:`); floor 0.10 → v 0.17 → vc 0.10 → dens 0.20
  → `.`/`'`; floor grid line 0.30 → v 0.51 → dens 0.6 → clearly visible;
  wall-top edge 0.75 → v 1.27 → `$`. That is the wanted grammar.
- Update `docs/terminal.md` formulas and the theme section; note the
  default change.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/ascii.test.ts tests/themes.test.ts` exits 0 and the full suite stays green
- [ ] Tests, by name (only these): a cell at `[0.03, 0.03, 0.05]` quantizes
      to the space glyph in cyber, gloom and solarized; `[0.14, 0.14, 0.15]`
      lands on a ramp index ≤ 18 (of 70) and `[0.75, 0.75, 0.75]` on
      index ≥ 60; a dark warm cell `[0.07, 0.065, 0.06]` gets a grey fg
      (`|r − b| ≤ 10` in 0–255) while a bright warm cell `[0.9, 0.6, 0.2]`
      keeps `r > b + 60`; `blackPoint: 0` and `gamma: 0.45` reproduce the
      old cyber output for a mixed buffer (regression path kept);
      overlay cells are unchanged; amber's white-input bloom result is
      unchanged.
- [ ] `docs/terminal.md` updated.
- [ ] No files changed outside scope

## Out of scope
- Renderer levels (a follow-up tidies the veil and corridor hues).
- UI, sprites, the ramp itself.

## Worker report

> claimed_by: opus-1 (pid 2044552) at 2026-09-03T22:59:51Z

Blocked before finishing tests + docs: the ticket's own default
`blackPoint = 0.08` and `gamma = 0.7`, plugged into the exact formula
in Context, do not produce two of the AC-named test thresholds. See
Questions below.

What is done (committed, branch `tigerteam/T-0028`):
- `src/render/ascii.ts` — new `QuantizeOptions.blackPoint`; defaults set
  to `gamma 0.7`, `blackPoint 0.08`; `cellFromRgb` rewritten to
  `vc = clamp((v − bp)/(1 − bp))`, `dens = vc^gamma`, then the desat
  fold `grey + (raw − grey)·sat` with `sat = clamp(dens·1.5)`, and the
  existing brightness fold `desat · clamp(dens·0.7 + 0.4)`. Overlays
  untouched. Amber branch threads the new `blackPoint` through.
- `src/render/themes.ts` — `amberDensity` and `amberMix` gain a
  `blackPoint` parameter (replacing the hard-coded 0.06). Same shape.

Not done pending decision on the questions below:
- `tests/ascii.test.ts`, `tests/themes.test.ts` — update expectations,
  drop redundant tests, add the new named cases.
- `docs/terminal.md` — new formulas + default change note.

Verification so far:
- `bash .tigerteam/scripts/run-tests.sh` — 185 passed, 1 failed
  (`render/themes > cyber output is byte-identical to the pre-theme
  formula for a mixed buffer` — expected: the pre-theme reference uses
  `gamma 0.45`, will be replaced by the AC-mandated
  `blackPoint: 0, gamma: 0.45` regression test).
- `npx tsc --noEmit` — clean.
- UI-fps fixture-replay tests all stayed green with the new defaults.

Commit: `c82a0de` on `tigerteam/T-0028`.

## Questions

Two of the named AC tests are unreachable with the spec's own defaults
(`blackPoint = 0.08`, `gamma = 0.7`, `exposure = 1.7`) plus the exact
formula in Context. Verified by running the formulas in JS:

| AC input           | actual `v` | `vc`   | `dens` | idx | AC wants     |
|--------------------|-----------:|-------:|-------:|----:|--------------|
| `[0.03,0.03,0.05]` | 0.085      | 0.0054 | 0.026  |  2 (`'`) | space (idx 0) |
| `[0.14,0.14,0.15]` | 0.255      | 0.190  | 0.313  | 22 (`}`) | idx ≤ 18      |
| `[0.75,0.75,0.75]` | 1.275      | 1.000  | 1.000  | 69 (`$`) | idx ≥ 60 ✓    |
| `[0.07,0.065,0.06]`| 0.119      | 0.042  | 0.109  | fg `[116,114,113]`, `|r−b|=3` ✓ |
| `[0.9,0.6,0.2]`    | 1.530      | 1.000  | 1.000  | fg `[255,170,57]`, `r−b=198` ✓  |

The Context's worked-example line "veil 0.03·1.7 = 0.05 → below the
black point → space" uses only the *single-channel* value 0.03 (v =
0.051 < 0.08). The AC test input is `[0.03, 0.03, 0.05]` whose max
channel is 0.05, giving v = 0.085 > 0.08 = blackPoint. Similarly the
worked example claims dens 0.29 (wall body) → "sparse `,`/`:`", but
`round(0.29·69) = 20` (`]`), not 6/7; and the AC threshold idx ≤ 18 is
only reachable with `blackPoint ≥ 0.13`.

The other three AC bullets (bright ramp, dark-warm grey fg, bright-warm
hue-preserving fg) and the four sidecar AC checks (regression path,
overlays, amber bloom, full suite) all pass with the spec defaults.

Options I see (any one unblocks):

1. **Fix the veil AC input to `[0.03, 0.03, 0.03]`** (matches T-0029's
   planned neutral veil `[0.03, 0.03, 0.03]`) and **relax the wall
   threshold to `idx ≤ 22`** (or ≤ 24 for margin). Keeps the spec
   defaults (`blackPoint 0.08`, `gamma 0.7`). Recommended — the AC
   input `…, 0.05]` looks like a typo referring to the *current*
   `unexplored` colour that T-0029 is about to rewrite.
2. **Raise the default `blackPoint` to 0.13** (or wherever). Both AC
   tests pass, but this contradicts the spec's explicit "default 0.08"
   and crushes more of the mid-dark range than the worked example
   suggests.
3. **Change the density mapping** to `dens = vc^(gamma·2)` (or use
   1/gamma). At `gamma = 0.7` this yields exponent 1.4, which does
   reproduce the worked example's `,`/`:` for wall body and space for
   both `[0.03,0.03,0.03]` and `[0.03,0.03,0.05]`. Breaks the AC's
   regression path (`blackPoint 0, gamma 0.45` no longer matches the
   old `dens = v^0.45` output).

Recommendation: option 1. Please pick.

> handoff: blocked

## Answers

Your arithmetic is right and mine was sloppy — thank you for checking it
instead of forcing the tests. Decision (a corrected option 1):

- Defaults: `blackPoint = 0.10`, `gamma = 0.9` (near-linear; 0.7 still
  lifts the darks more than the look needs). Keep the mapping
  `dens = vc^gamma`, the regression path (`blackPoint 0, gamma 0.45`
  reproduces the old output) and everything else as you built it.
- Corrected acceptance numbers with those defaults (exposure 1.7, 70-glyph
  ramp): `[0.03, 0.03, 0.03]` and `[0.03, 0.03, 0.05]` both → space
  (v ≤ 0.085 < 0.10); wall body `[0.14, 0.14, 0.15]` → ramp index ≤ 16;
  floor `[0.10, 0.10, 0.11]` → index ≤ 9; `[0.75, 0.75, 0.75]` → index ≥ 60.
  Update the three tests to these thresholds and fix the worked-example
  numbers in `docs/terminal.md` to match (wall body ≈ 0.19 density, floor
  ≈ 0.10, veil 0).
Then hand off to review.


> claimed_by: opus-1 (pid 2044552) at 2026-09-03T23:14:53Z
